# Models

## Book

`Book` maps to `library_books`, overriding the naming convention on purpose - see
[architecture](architecture.md) for why. Everything else in the catalog follows the convention
Eloquent would pick on its own.
