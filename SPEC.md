# Mapin - Specification

**Status:** draft v0.1, pending approval
**Package:** `nexvia-solutions/mapin`
**Namespace:** `Mapin`
**License:** MIT
**Language policy:** code, comments, commits and README in English. `README.es.md` carries the Spanish translation.

---

## 1. Objective

Mapin builds a queryable graph of a Laravel application: files, classes, methods, routes, views, models, tables, jobs, events and documentation, connected by the edges that actually matter in Laravel (dependency injection, container bindings, route to controller, controller to view, Blade includes, Eloquent relations, dispatched jobs and events).

Generic code graph tools (tree-sitter based) only see what is visible in the syntax of a single file. In Laravel, most wiring happens through the container, through strings (`route('name')`, `view('name')`) and through conventions (`hasMany`, `$listen`, `Route::get`). Measured on a real application of about 900 PHP files, a generic extractor captured static calls and nothing else: zero edges for injection, `new`, member calls, routes, views, Blade or relations. Mapin exists to cover exactly that gap.

### 1.1 Target users

1. **AI coding agents** (Claude Code, Cursor, any MCP client) that need to answer "what breaks if I change this" and "where is this used" with low token cost and honest uncertainty.
2. **Developers** doing impact analysis, onboarding or refactoring on a Laravel codebase they do not fully know.
3. **Teams** that keep architecture docs in Markdown and want them linked to the code they describe.

### 1.2 Success criteria (measurable)

| Criterion | Target |
|---|---|
| Member calls whose receiver resolves to a class declared in the project (not a library) | 100% resolved at confidence 0.7 or higher; at least 15% at confidence 0.8 or higher |
| Member calls overall (project and library receivers combined) | at least 75% resolved at any confidence, at least 45% at confidence 0.8 or higher |
| Full build on about 1,000 PHP files plus 650 Blade views | under 60 seconds on a 2 core machine |
| Incremental rebuild after touching one file | under 5 seconds |
| Route to handler coverage | 100% of routes reported by the router |
| `impact` query for a method returns the routes, views, jobs and docs that reach it | in one call, grouped by type, with confidence |
| Not-found queries | always return `found: false`, never a substituted fuzzy match |
| Staleness | every answer carries the commit the graph was built from and the number of files changed since |

### 1.3 Non-goals (what Mapin does not cover)

Mapin is static analysis with a read-only peek at the booted container. It does not and will not cover:

- Targets computed at runtime: `view($name)`, `route($x)`, `app($class)`, `$model->$relation()`, `call_user_func`, `__call`. These are recorded as unresolved with the expression text.
- Full data flow through local variables. Chains are followed only through declared return types and a small set of Laravel-aware rules.
- Conditional container bindings (per environment, per tenant). The binding present in the booted environment is recorded.
- Eloquent magic by property access (`$order->customer`) unless the receiver type is known; accessors, casts and dynamic scopes.
- Anything stored in the database: enabled modules per tenant, permissions, feature flags, menu tables.
- Raw SQL at column level. `DB::table('x')` and `Schema::create('x')` yield table names only.
- JavaScript in depth. Phase 7 maps HTTP calls in JS files to routes by URL; component trees and state flow are out of scope.
- Runtime behaviour: real dead code, executed paths, performance. "No callers found" is never proof that something can be deleted.
- Third-party code in `vendor/`. References to it become `external` nodes without content.
- Business meaning. The Markdown module links docs to code; it does not infer intent from code.

### 1.4 Phase 1 results, and the incremental invalidation gap (closed)

Phase 1 built the package described in section 14's phase table: the extraction and resolution pipeline from the spike, now real code behind `Extractor`, `SymbolIndex` and `Resolver`, persisted to SQLite, driven by `mapin:build` and queried by `mapin:stats`, `mapin:find` and `mapin:callers`. It was verified two ways: 13 Pest tests (9 unit tests exercising every resolver rule in section 4.1 against an invented fixture app with no Laravel booted, 4 feature tests running the real Artisan commands through Orchestra Testbench) all pass, and `bin/benchmark.php` was run against the real Laravel application from section 4.2 (3,596 files once the vendor paths it indexes are included): a full build took 33 to 43 seconds, an incremental build after touching exactly one file took 0.14 seconds, and an incremental build with nothing changed took 0.87 seconds. Both timing targets in section 1.2 are met with comfortable margin, on a codebase over three times the file count the target names.

Three bugs the tests caught before this reached anyone are worth recording, because they are exactly the kind static review would not have found: `SymbolIndex::isInterface()`-driven substitution was implemented in the chain-continuation path (`returnOf`) but not in the direct edge-recording path (`record`), so a call straight through an injected interface was never substituted with its concrete implementation, only a further chain off that call was; a synthetic `relation<kind,Target>` marker returned by a found method was mislabelled with the generic `return_type` resolution instead of `eloquent`, understating its confidence; and `FileDiscovery`'s vendor-directory exclusion matched the full absolute filesystem path rather than the path relative to the project root, so every file was silently excluded whenever the project itself happened to live under a directory literally named `vendor` - which is exactly Testbench's own layout, not a contrived edge case.

**The gap (as first shipped, now closed).** Incremental correctness was not complete against the design in section 5: when a changed file's declaration affected a symbol used by an *unchanged* file - a method gaining a return type, say - the unchanged file's already-stored edges were never re-resolved, because nothing tracked which stored edges depended on which target symbols. `mapin:build --full` was always fully correct; incremental builds were fast and correct for the common case but could leave a stale edge in an unrelated file until the next full build.

**The fix.** `SymbolIndex` now records, on demand (`startTracking()`/`stopTracking()`), every symbol any lookup method touches - `get()`, `has()`, `singleImplementation()` and `functionReturn()` are the only places a symbol is ever consulted, and every other lookup (`findMethod`, `findProp`, `isModel`, `isInterface`, ...) is built on top of them, so instrumenting these four is enough to capture a reference's complete dependency set regardless of how deep the resolution recursed. `ReferenceVisitor` wraps each of its four top-level reference sites (member call, static call, `new`, `app()`/`resolve()`) in a tracking scope and attaches the result to the `Edge` or `UnresolvedRow` it produces (`Edge::$dependsOn`, `UnresolvedRow::$dependsOn`). `SqliteStore::symbol_deps` stores this as a plain `(file_id, symbol)` table, replaced per file on every (re-)resolution. Before an incremental build touches anything, it reads each changed file's previously stored declared symbols (each class's own FQCN, its interfaces, each function's name) and unions that with what the fresh extraction now declares - additions, removals and changes are all covered by this union - then queries `symbol_deps` for every other file depending on any symbol in that set. Those files are re-parsed and re-resolved alongside the genuinely changed ones, reported separately as `files_affected` in the build report (`BuildReport::$filesAffected`), and this closes the loop with no transitive second pass needed: an affected file's own declared symbols never change (only its outgoing references are re-resolved), so nothing it declares can itself need a further round.

Verified with a feature test (`tests/Feature/IncrementalInvalidationTest.php`) built specifically to fail without the fix, and confirmed to actually fail when the fix was temporarily disabled: a method with no return type is called across two files; a chained call off its result is unresolved; the method gains a return type in an incremental build that touches only its own file; the chained call in the *other*, untouched file resolves on the very next build, and that file's stored content hash is asserted unchanged throughout, proving it was never treated as "changed" - only re-resolved because it depended on what did change.

Measured against the real application from 4.2: touching a single static-utility class called from dozens of places across the codebase produced 58 affected files and took 1.46 seconds incremental - still comfortably inside the 5 second target, and the case this mechanism exists for.

One real bug surfaced only at this scale, not in the small fixture: `array_diff()` preserves the original array keys of survivors, and `PDOStatement::execute()` with positional `?` placeholders requires a sequential 0-indexed array - the first version of the affected-files query threw "column index out of range" the moment `array_diff` actually removed an element from the middle of a real, multi-file result set, something a one- or two-file fixture test never exercised. Every method in `SqliteStore` that builds an `IN (...)` clause now normalizes its input array with `array_values()` at the top, not just at whichever call site happened to remember to.

Not yet done, tracked rather than skipped: the CI workflow (`.github/workflows/ci.yml`) is written but has not run on real GitHub Actions infrastructure; the golden-test apparatus in section 12 (a single `tests/Golden/expected.json` diffed against build output) is not built yet, the current tests assert specific expected edges directly instead; and the fixture app covers the rules in section 4.1, not the full exhaustive pattern list section 12 describes for later phases (routes, Blade, jobs, events, migrations are phase 2 extractors that do not exist yet).

---

### 1.5 Phase 2 results

Phase 2 added the Laravel-specific extractors from section 14's phase table: `RouteExtractor` and `BindingExtractor` (both `requiresBoot(): true`, reading the booted `Router`/`Container` directly, no source parsing), `BladeExtractor` (regex-based against `@extends`/`@include`/`@includeIf`/`@each`/`@includeWhen`/`@includeUnless`/`@includeFirst`/`@component` and `<x-*>`/`<x:*>` component tags - not the "compiler based plus fallback" the phase table originally named; regex was enough to pass every case tested and is simpler, so the compiler-based approach was not built until 1.21), plus new detection inside the existing `PhpExtractor`/`ReferenceVisitor` pair: Eloquent relation methods (`relates`), table naming convention and `$table` override (`maps_table`), migration `Schema::` calls (`touches_table`), job/event dispatch (`dispatches`), `Event::listen()` and provider `$listen` properties (`listens`), `Model::observe()` (`observes`), `Schedule::command()`/`->job()` (not yet asserted by a dedicated test, built alongside the others per the same pattern). `mapin:route`, `mapin:view`, `mapin:model` and `mapin:impact` were added, `mapin:impact` being the phase's gate centerpiece: a BFS over 13 reverse edge types, classifying each result into routes/views/jobs/commands/methods/classes with a one-hop heuristic (checks `meta.interfaces` for a `ShouldQueue` suffix, `meta.parent` for a `\Command` suffix) - a lightweight stand-in for the full class-category system section 3.4 describes, not that system itself.

Verified with 27 Pest tests (the 15 from phase 1 plus 5 more unit tests for relation/table/migration/dispatch/observer/listener detection against the invented fixture app, still unbooted; 7 feature tests running `mapin:build` through a real Orchestra Testbench application boot - real `Route::get()`/`Route::post()` registration, a real container `bind()`, real Blade files on disk) - all green, PHPStan level 6 and Pint both clean.

Two real bugs, again exactly the kind a passing test suite with loose assertions does not catch on its own:

**Forward-reference edges silently dropped.** `BuildRunner`'s per-file loop upserted one file's nodes, then immediately resolved that same file's edges' target keys to database IDs, then moved to the next file - so an edge from file A to a class declared in file B, processed later in the same build, looked up an ID that did not exist in the store yet and was silently dropped (`insertEdges` skips edges it cannot resolve an ID for, by design, since a resolution can legitimately point outside the indexed set). This was invisible in phase 1 because DAGs of extraction order rarely mattered for the smaller edge set tested there; phase 2 surfaced it hard, because `RouteExtractor`/`BindingExtractor` ran and tried to resolve their `routes_to`/`binds` edges' target class nodes *before the per-file node-upsert loop had run at all* - on a fresh build every one of those edges was dropped, not just the unlucky ones. Root-caused by instrumenting `BuildRunner` with temporary `fwrite(STDERR, ...)` tracing (removed before this section was written) that printed the node-ID lookup result for every edge from one fixture method, showing `fromId=<real id>, toId=MISSING` for every single cross-file target. Fixed by splitting the build into three ordered passes inside the same overall build: upsert every file's own nodes first (its own transaction), *then* run route/binding extraction (their targets now exist), *then* resolve each file's references and only look up target IDs once every node any of them could point at - across every file *and* the routes/bindings - has been upserted.

**Relation targets stored under the wrong key.** `relates`, constructor-assignment property types, and the `$listen` provider property were all extracted via `PhpParser\NodeFinder`, searching a method's or property's already-parsed subtree for a matching node - which runs entirely outside the shared `NodeTraverser`'s own traversal, so `NameResolver` (registered ahead of the extraction visitor in that same traverser) had not yet reached those nested `Name` nodes and had not resolved them. A relation like `$this->hasMany(Book::class)`, with `Book` imported via `use App\Models\Book;`, extracted the edge's target as the literal string `Book` rather than `App\Models\Book` - a node key nothing in the graph actually declares, so the edge pointed nowhere and (after the fix above) failed the ID lookup exactly like a real forward reference, just for a different underlying reason. Every one of phase 1 and phase 2's own unit test assertions for these edges used `str_contains($edge->toKey, 'Book')` rather than an exact match, and a bare `"Book"` satisfies that substring check exactly as well as `"App\Models\Book"` does - so the bug shipped with the tests that should have caught it passing throughout. Root-caused by comparing a direct, no-database call into `PhpExtractor`/`Resolver` (which produced the correct FQCN) against the same fixture run through `mapin:build` (which produced `class:Book`), then tracing `DeclarationVisitor`'s three `NodeFinder`-based call sites by hand. Fixed with a `resolveName()` helper on `DeclarationVisitor` that applies the same `uses`/namespace resolution `NameResolver` would have, used at all three sites (`findRelationCall`, `scanConstructorAssignments`'s two branches, `classConstName`). The existing test assertions were left as `str_contains()` - they still pass, now for the right reason - rather than widened while fixing an unrelated bug in the same pass.

Measured against real code, not just the fixture: an unbooted, read-only extraction pass (no Laravel boot, so `routes_to`/`binds` are not exercised here) against a snapshot of the application from section 4.2 - 1,599 PHP and Blade files under `app/`, `routes/`, `config/`, `resources/views/` - completed in 12.3 seconds, producing 11,239 nodes and 39,999 edges (23,348 `calls`, 6,978 `declares`, 732 `relates`, 730 `includes`, 545 `instantiates`, 527 `renders`, 323 `touches_table`, 260 `maps_table`, 229 `extends`, 60 `resolves`, 47 `uses_trait`, 23 `dispatches`, 11 `implements`, 7 `observes`, 7 `uses_component`) and 666 `view` nodes. Sampled `relates` edges at this scale resolve to full FQCNs correctly, confirming the `resolveName()` fix holds beyond the fixture, not just on the case it was written against. One file failed to parse (a pre-existing PHP syntax error unrelated to Mapin) and was correctly reported as a build warning rather than aborting the run.

**The booted-router gap, closed the same day.** `RouteExtractor`/`BindingExtractor` were then run against the same real application, this time fully booted - its own `Illuminate\Foundation\Application`, its own `vendor/autoload.php` required after Mapin's own (so Mapin's own dependency versions win for anything it ships, while app-only classes like `Illuminate\Foundation\Application` still resolve), the real console kernel bootstrapped exactly as `artisan` itself does it, against its own already-running local dev database (not shared, not production). This surfaced one real crash, not caught by any fixture: `RouteExtractor` assumed every entry in `Route::gatherMiddleware()` was a string alias, but Laravel also allows registering a middleware as a bare `Closure` directly, and this application does that on at least one route - `Cannot access offset of type Closure on array` at the exact line indexing `$middlewareAliases` by it. Fixed by skipping non-string middleware entries: a closure has no name stable across builds to key a graph node on, so there is nothing to record for it, the same reasoning already applied elsewhere for anything without a resolvable identity.

With that fixed, the gate: **1,589 route nodes in the graph against 1,589 routes in the real, booted `Router::getRoutes()` - exactly matching, the phase's "100% of router routes present" wording met precisely, not approximately.** `routes_to` linked 1,536 of 1,579 controller-backed routes (the other 10 of 1,589 are closures, which correctly get no `routes_to` edge at all - there is no controller method to link to). The 43 unlinked controller-backed routes were sampled, not exhaustively audited - 11 were checked by hand - and every one checked resolved to either a route correctly outside Mapin's project scope (a `Route::controller()` group here wraps its actions in closures for a deliberately disabled module, and Laravel's own `route:list` reports the *group's* controller class as the action name with no `@method` even though the actual registered handler is a closure - `controllerTarget()` correctly declines to link this, since there genuinely is no controller method involved) or - the larger bucket - a route whose action names a method that grep confirms does not exist anywhere in `app/` at all: a real, pre-existing dead route in the application itself (would 500 if ever requested), not a Mapin gap. `binds` produced only 3 edges, all framework-internal (`Illuminate\Contracts\Console\Kernel`, `Illuminate\Contracts\Http\Kernel`, `Illuminate\Contracts\Pipeline\Hub`) - confirmed correct, not a gap, by grepping the real `app/` for `->bind(`/`->singleton(`: zero results, this application does not use explicit container bindings anywhere in its own code.

Not yet done, tracked rather than skipped: `BladeExtractor` is regex-based, not compiler-based, as noted above; the golden-test apparatus from section 12 is still not built; `mapin:impact`'s job/command classification is the one-hop heuristic described above, not section 3.4's full category system; and the 14,793 unresolved rows the unbooted real-codebase run produced were not analyzed the way phase 0's spike numbers were - phase 2 did not repeat that measurement discipline, so there is no confidence rate to report here the way section 4.2 reports one for phase 0.

### 1.6 What stands between phase 2 and practical daily use

Assessed 2026-09-07, at Brandon's request, for what it would take to actually rely on Mapin day to day rather than as a project that passes its own tests. Consolidated here as one list because the individual items are otherwise scattered across 1.4, 1.5 and section 14 - this section adds nothing new to those, except the installation gap, which was not tracked anywhere before now.

1. ~~No MCP server yet (phase 3 itself).~~ **Closed the same day, see 1.7** - `mapin:mcp` now exists and was verified with a real JSON-RPC round-trip, not just the CLI.
2. ~~Not installed anywhere it would actually be used.~~ **Closed in 1.9** - real `require-dev` entry in a real host application (on its own Laravel-upgrade branch, not its main working branch), real `.mcp.json` entry, checked by actually running it. No build hook analogous to the previous tool's post-deploy rebuild exists yet, though - `mapin:build` still needs to be run by hand, or from a manually-added git hook (section 16's open question about `--install-hook`).
3. ~~`BladeExtractor` is regex-based, not the compiler-based approach section 4 originally named - see 1.5.~~ **Closed in 1.21** - the regex scanner (renamed `DirectiveScanner`) stays the structural source, composed inside a new compiler-based `BladeExtractor` that also resolves `route()`/`view()`/method calls written as literal PHP inside a template, the real gap 1.8 named below.
4. **No golden-test apparatus** (section 12) - regressions are only caught by the specific edges each test happens to assert, not a broad snapshot diff.
5. **~27% of references in the real, unbooted benchmark landed in `unresolved`, unaudited** - phase 0's spike bucketed every unresolved case by cause before calling the number acceptable; phase 2 has not repeated that discipline, so this fraction is unexplained, not necessarily concerning.

---

### 1.7 Phase 3 results

Built the same day, immediately after 1.6 identified the MCP gap as the highest-value remaining piece. `Mapin\Query\Query` is now the one service backing every tool (SPEC.md section 6): one method per tool in section 6.1's table (`find`, `node`, `callers`, `callees`, `impact`, `path`, `route`, `view`, `model`, `unresolved`, `stats`), each returning a `QueryResult`, plus `envelope()` wrapping that in the `{found, query, result|suggestions, graph}` contract - including the `graph` staleness block (`built_commit` vs. `head_commit` via `Mapin\Query\GitStatus` shelling out to `git`, `files_changed_since_build`), which section 8 always described but nothing built until now. `find`, `callers`, `route`, `view`, `model`, `impact` and `stats` (the existing CLI commands from phases 1-2) were refactored onto this shared service rather than left with their own ad-hoc query logic and response shapes; `node`, `callees`, `path` and `unresolved` are new. `mapin:query {tool} --arg=key=value` is the generic dispatcher section 9 describes, driving the exact same `ToolSpec` instances the MCP server calls - a CLI answer and an MCP answer to the same question share code, not just a contract.

The MCP side is `laravel/mcp` v1.0.0-beta.1 (its actual installed API read directly from `vendor/laravel/mcp`, including its own Boost skill documentation, rather than assumed from training data, given its beta status flagged as a risk in section 15) behind the two-layer split section 6.1 always specified: `src/Mcp/Tools/*` (11 `ToolSpec` implementations, one per tool, each depending only on `Query`) and `src/Mcp/Adapters/*` (an `AbstractToolAdapter extends Laravel\Mcp\Server\Tool` handling schema-building and request/response translation generically, plus one one-line concrete subclass per tool - the only classes that import anything from `laravel/mcp` outside the adapters directory itself). `MapinServer extends Laravel\Mcp\Server` registers all 11. `mapin:mcp` starts it over stdio directly - `new StdioTransport; $server->start(); $transport->run();`, mirroring `Laravel\Mcp\Server\Registrar::startServer()`'s own internal pattern - so a host app needs nothing registered in its own `routes/ai.php`; the command is self-sufficient, matching section 9's listing. `mapin:doctor` checks PHP/extension requirements, whether the router is bound, graph existence and staleness, and the unresolved ratio.

Verified with 9 new Pest tests (`tests/Feature/Phase3Test.php`) exercising the real MCP protocol path via `laravel/mcp`'s own testing helper (`MapinServer::tool(SomeToolAdapter::class, [...])->assertOk()->assertStructuredContent(...)`, the pattern the package's own documentation recommends) - not a mock, an actual `tools/call` JSON-RPC request handled by `CallTool`/`ToolInvoker` and answered through `AbstractToolAdapter::handle()`. Covers: the full envelope shape including the `graph` block, a route lookup resolving through to a real controller method, an impact query, a no-argument stats call, the `found: false` + labelled-`suggestions` contract for a name that matches nothing, and `find`'s exact-vs-fuzzy behavior. Two more tests drive `mapin:query` and one drives `mapin:doctor`. All 36 tests in the suite (27 carried over from phases 1-2, 9 new) pass, PHPStan level 6 and Pint are clean. Beyond the automated suite, the exact stdio wiring `mapin:mcp` uses (`StdioTransport` + `MapinServer::start()`, not the test helper's separate code path) was checked by hand with a raw JSON-RPC string fed straight to the transport's registered handler inside a real Testbench boot - confirmed a well-formed `tools/call` response comes back with the full envelope nested correctly under `structuredContent`, so the command itself, not just the tool logic behind it, is known to work.

Three real issues surfaced building this, not from imagining edge cases in advance:

1. **`find` needed a genuine behavior change, not just a new response shape.** Every existing caller of `findNodes()` used its LIKE-based matching unconditionally - exactly the "approximate match with full confidence" failure mode section 6's "never answer with an approximate match without saying so" rule exists to prevent. Query::find() now requires an exact hit (key, full name, or - since a class node's own `name` is its FQCN, not its short name - the segment after the last backslash) unless `fuzzy: true` is passed, with LIKE-based hits demoted to labelled `suggestions` otherwise. The first version of this was too strict: it initially failed a phase 1/2 test doing a plain short-class-name search, because "short class name" is explicitly one of `find`'s exact-match targets in section 6.1's own table, not something that should need `fuzzy: true`.
2. **The container didn't know how to build `Query` or `SqliteStore`.** CLI commands construct both manually (`new Query(new SqliteStore($storagePath), base_path())`) because they already have `$storagePath` and `base_path()` in hand. `laravel/mcp`'s `ToolInvoker` resolves a tool's `handle()` through `Container::getInstance()->call(...)`, which needed both bound as singletons in `MapinServiceProvider::register()` - caught immediately by every MCP test throwing `BindingResolutionException` before the binding was added, not a subtle bug.
3. **The known `docker cp` directory-nesting pitfall (documented in this project's own memory from phase 1) bit again, compounded by a bash heredoc escaping mistake.** Generating the 11 adapter classes from a shell loop produced a broken `use Mapin\Mcp\Tools${tool};` line (a literal, unexpanded `${tool}`) in every one of them - an escaping error in the heredoc. Fixed on disk, then synced with `docker cp .../Adapters dev-container:/tmp/mapin/src/Mcp/Adapters` (no trailing `/.`/`/`) - which, because source and destination both end in `Adapters`, nested the whole directory one level deeper instead of overwriting it, so PHPStan kept reporting the exact same syntax error against the untouched stale copy underneath. Re-synced correctly (`rm -rf` the container's copy first, then `docker cp .../Adapters/. dev-container:/tmp/mapin/src/Mcp/Adapters/`) before it was traced to two separate mistakes stacked on each other rather than one.

Not yet done, tracked rather than skipped: `hubs`, `communities` and `docs` are not registered on `MapinServer` - they need phase 4/5 data (Markdown module, community detection) that does not exist yet, so leaving them out is accurate, not a stub; `impact`'s `types?` filter from section 6.1's table was never implemented (`ImpactCommand` didn't have it before this phase either - not a regression, just still open); and the MCP server has only been verified against Testbench's invented fixture, not run against a real application the way phase 2's extractors were - closing that gap, and installing Mapin somewhere it would actually be reached from a normal session (section 1.6 item 2), are the two concrete items left before this is more than a project that passes its own tests.

---

### 1.8 Hardening pass

The real application from section 4.2 was mid-edit in another working session at the time, so validating Mapin against it (the two items 1.7 closed with) was deliberately deferred; this pass closes debt already on the books instead, at Brandon's explicit request to harden rather than add new phase surface while that window is closed.

**The golden-test apparatus from section 12 now exists.** `tests/Feature/GoldenTest.php` builds the fixture app in full and dumps every node key and every `{type, from, to, resolution}` edge, sorted for determinism, diffed against a committed `tests/Golden/expected.json` (103 nodes, 97 edges as of this pass) - regenerated deliberately with `MAPIN_UPDATE_GOLDEN=1`, never to silence a failure without first understanding why the graph changed shape. Building this required first auditing the fixture app against section 12's own exhaustive pattern list, since a golden snapshot of an incomplete fixture only locks in what was already being tested, not what section 12 actually asks for.

That audit found real gaps, not just missing test decoration - four patterns had working extractor/resolver code behind them with zero coverage anywhere, and one had no code behind it at all:

1. **Invokable controllers routed to nothing.** `RouteExtractor::controllerTarget()` treated any action without an `@method` as an unresolvable closure - true for a real closure, but also exactly the shape of `Route::get($uri, SomeInvokableController::class)`, a legitimate, common Laravel pattern. Fixed to resolve the bare-class case against the controller's own `__invoke` method, the same way Laravel itself does at dispatch time. This is the one item in this pass that is a genuine bug fix, not just added coverage - verified with a dedicated fixture (`Fixture\Http\Controllers\ReportController`) and its own test, independent of the golden snapshot.
2. **`$table` override had no fixture exercising it at all** - `Book` now declares `protected $table = 'library_books';`, verified against the naming-convention case already covered by `Author`.
3. **Scheduled-command detection (`Schedule->job()`/`->command()`) had no fixture or test since it was built alongside the rest of phase 2's detection code** - added `Fixture\Providers\BookScheduleProvider` and a dedicated test.
4. **Middleware alias resolution to its bound class had no fixture or test** - added `Fixture\Http\Middleware\EnsureTenant`, aliased and applied to a new route, with a test asserting the middleware node's `meta.class` resolves correctly, not just that the edge exists.

Wiring the invokable-controller fixture surfaced a second, unrelated gap: `Fixture\*` classes had never had a real PSR-4 autoload mapping (`composer.json`'s `autoload-dev` only mapped `Mapin\Tests\` to `tests/`) - every existing test worked anyway because `SomeClass::class` and `[Controller::class, 'method']` route actions never trigger autoloading, only a bare-class-string route action does (Laravel calls `method_exists()` on it immediately at registration, which does trigger autoloading). Added `"Fixture\\": "tests/Fixtures/app/"` to `autoload-dev.psr-4` - a fixture app whose classes were never really autoloadable was itself a small, latent gap between "the fixture behaves like a real Laravel app" and what was actually true.

Four new named tests (`tests/Feature/HardeningTest.php`) assert each of the four patterns above independently of the golden snapshot - discoverable and specific if the golden file ever needs regenerating for an unrelated reason, rather than relying on a diff of a 200-line JSON file to communicate what changed and why. All 41 tests (37 carried over, 4 new, golden test included) pass, PHPStan level 6 and Pint are clean.

**Deliberately not done in this pass**, to keep it bounded rather than open-ended: section 12's fixture list also asks for method injection (as distinct from constructor injection), a static factory method, Eloquent local scopes, every relation kind (only `hasMany`/`belongsTo` are exercised, of the nine `RELATION_KINDS` supports), route groups and `Route::resource()`, and untyped constructor injection - none of these have a known gap in the underlying code the way the four items above did, so adding fixture coverage for them is lower-value busywork rather than hardening, and was left for whenever it is next convenient rather than done here for completeness's own sake. `route()`/`view()` calls written inside a `.blade.php` file (as opposed to PHP) are not detected by `BladeExtractor` at all - a real, not cosmetic, gap, but building that detection is new extractor surface, not hardening of what exists, so it was named rather than built; see the open item below. Closed in 1.21.

---

### 1.9 The two phase 3 gaps, closed

Done the same day, once Brandon's other working session finished editing the real application from section 4.2 and the window this whole spec keeps insisting on respecting (never touch a repo mid-edit in another session) opened back up.

**Gap 1: the MCP server, run for real, not just against the fixture.** Booted the real application (read-only - never wrote anything under its own path beyond Mapin's own throwaway build during this check) and drove `MapinServer` over the exact `StdioTransport` wiring `mapin:mcp` uses, sending real `tools/call` requests. `find` resolved a real class by short name; `route` returned a real route's full middleware stack (nine aliases deep) and the view it renders; `impact` on the same heavily-called static method phase 0's spike measured at 104 call sites correctly listed 20+ real routes reachable from it at depth 2; `stats` and the `graph` staleness block both reported correctly. This is the confirmation phase 2's extractor-level validation could not give on its own: the full query layer, over the actual MCP wire protocol, answering real questions about real code.

That same check surfaced a fourth real bug, in `model()` this time, not caught by the fixture because the fixture has no two classes whose short names collide with a search term the way real, large-scale code does: searching for a real Eloquent model by its short name returned an unrelated console command whose class name happened to contain the same substring, instead of the actual model. `Query::model()` had never been updated to use the exact-short-name matching `find()` got in 1.7 - it just took `findNodes()`'s first LIKE-ranked row unconditionally, so a search term matching several classes by substring, none of them exactly, silently returned whichever one SQLite happened to return first. Fixed by extracting `find()`'s matching rule into a shared `Query::exactMatches()` and using it in `model()` too; unlike `find()`, `model()` has no `fuzzy` argument to fall back to (section 6.1 gives it only `name`), so anything other than exactly one exact match - zero, or more than one short name colliding across namespaces - is now reported as not found with candidates, never a silent pick. Covered by a new fixture-based regression test reproducing the same ambiguity ("Controller" matches four fixture controllers, none of them exactly). 42 tests total, PHPStan and Pint clean.

**Gap 2: installed somewhere a normal session actually reaches it - genuinely, not a demo.** This surfaced a real blocker before it surfaced a real result: the host application's main working branch is still on Laravel 10 (`laravel/framework: ^10.8`), and Mapin's own compat floor is `illuminate/* ^11.45|^12.0|^13.0` - a version conflict, not a workaround-able one, since `laravel/mcp` and the JSON Schema support it needs are themselves Laravel 11+-only. A Laravel 10→13 upgrade this project's own compat decision assumed already existed turned out to live on unmerged branches instead (one per target version - Laravel 11, 12 and 13 - all committed the same day). Installed against the Laravel 13 branch (PHP 8.3, matching the notebook's own container PHP version exactly), at Brandon's explicit choice over waiting for the merge or lowering Mapin's own floor.

Getting there took three real infrastructure changes, not just a `composer require`:

1. `~/mapin` was not visible inside the host application's own dev container at all (it only ever mounted the host application's own working copy) - a `composer path` repository needs the target directory actually readable from wherever `composer install` runs. Brandon chose mounting over copying Mapin's source into the host application's own tree, so `~/mapin` is now bind-mounted into that container at the identical absolute path, added to the notebook-dev pilot project's own compose file (a separate, personal "one stack per developer" setup, not the host application's own compose files) and applied with a scoped, single-service recreate, not a full stack restart.
2. Mapin's own git repo has zero commits (still true - "git init hecho, sin commits" from the very first day of this project). A `path` repository still works against an uncommitted git working tree, but Composer reports its version as the synthetic `dev-main`, which `minimum-stability: stable` rejects outright, and which then exposed that `laravel/mcp` itself is only available as a beta release with no stable tag - a `stability-flags` entry does not apply to a transitive dependency pulled in by another package, only to something required directly, so the fix was project-wide `minimum-stability: beta` (kept safe by the `prefer-stable: true` already in place, which still prefers a stable release for every other package that has one).
3. Both the host application's own working copy and `~/mapin` needed a git `safe.directory` exception inside the container - bind-mounted repos owned by the host user look "dubious" to git running as the container's own user, and Composer shells out to git for path-repository version detection.

`nexvia-solutions/mapin` is now a real `require-dev` entry, symlinked from `~/mapin`, and `mapin:build`/`mapin:doctor`/`mapin:mcp` all run as ordinary `artisan` commands against the host application's own real graph at `storage/mapin/graph.sqlite` (added to `.gitignore` - a rebuildable artifact, never meant to be committed). The host application's own `.mcp.json` (already tracked, already had an unrelated MCP server entry) got a `mapin` entry running `mapin:mcp` inside its own container - verified by running that exact command line with a piped raw JSON-RPC request, not just trusting the config, so this is the same command a client actually invokes, checked end to end, not merely declared.

**One thing to flag, not hide:** closing this gap required checking out the Laravel 13 upgrade branch in the host application, so Brandon's working branch there changed as a side effect of this work, not something decided in advance - worth knowing about before doing anything else there.

**A second, unrelated operational gap surfaced once the dev container was recreated for gap 2's mount** (this section, above): `phpstan.neon` had never actually made it into `~/mapin` on the host at all. Every "PHPStan clean" claim across this entire spec, from phase 0 onward, was true against a config file that only ever existed inside the container's ephemeral working copy - never synced back alongside `src/`, `tests/` and `config/` the way this project's own established sync habit called for, because sync passes were always scoped to those specific directories, never root-level files. It surfaced now, not earlier, because recreating the container for gap 2's mount destroyed that ephemeral copy for good. Reconstructed from scratch (`level: 6`, `paths: [src]` only - confirmed by process of elimination: adding `tests/` or `bin/` back in immediately produced 33 and 1 pre-existing errors respectively, neither of which this spec ever reported, so neither was ever actually in scope) and verified clean again. A companion `pint.json` (also never present before) now excludes `spike/` for the same reason - `spike/resolve.php`'s one style violation was real and pre-existing, just never caught, since Pint's own cache file lived in the same now-gone ephemeral copy. Both are ordinary config files now committed to the tree like everything else, not a special case going forward - this was a one-time consequence of relying on a disposable location as the only home for either of them.

---

### 1.10 Phase 4 results

Built at Brandon's explicit request ("arranquemos con la Fase 4 (Markdown)"), the deterministic half of section 7 only - the semantic/LLM half (`LlmClient`, `NullClient`, `--with-llm`) is phase 6 and was not touched.

**`Mapin\Extract\Markdown\MarkdownExtractor`** is not an `Extractor` implementation, for the same reason `RouteExtractor`/`BindingExtractor` are not: it needs more than one file's own content. It is split into `nodes()` (pure text parsing - a `doc` node per file, a `section` node per heading with a GitHub-style unique anchor, no store access) and `edges()` (`declares` from doc to section, `links_doc` for relative Markdown links, `documents` for a mention in section text that resolves to exactly one real node), mirroring `PhpExtractor`/`Resolver`'s own declare-then-resolve split and for the identical reason: a same-page anchor link, or a link to another Markdown file in the same build batch, needs that target's own `doc` node to already exist, which is only true once every file's `nodes()` pass has run, not just this one's. Wired into `BuildRunner`'s existing three-pass structure (section 2) rather than as a second, parallel pipeline: `config('mapin.paths')` already defaulted to include `docs`, so Markdown files go through the exact same discover-hash-changed-files cycle as PHP and Blade, for free inheriting incremental invalidation and the `graph` staleness block - a doc file edited since the last build is "changed" exactly the way a controller is.

Match resolution is exact-only, the same standard section 6's whole "never answer with an approximate match without saying so" rule sets elsewhere: a backticked or plain class mention resolves via the identical key-or-full-name-or-short-name rule `Query::exactMatches()` uses for `find()`/`model()` - reimplemented rather than shared across the extract/query boundary on purpose (build-time over the graph being built, read-time over a finished one; documented in `MarkdownExtractor`'s own docblock, not an oversight). A dotted name checks both a route name and a view name and keeps every hit; a leading `/` checks every route URI regardless of HTTP method (`SqliteStore::findRoutesByUri()`, new - `findRouteByName()` moved from a private duplicate in `Query` to a shared public method on the store, same rename). File paths and table names are gated behind backticks specifically - the two least distinctive shapes (a bare identifier, or a path-looking string), given a `.md` file's own prose is far more likely to contain incidental text shaped like either than shaped like an FQCN. Zero matches is not recorded at all (most capitalized prose is not a reference to project code); more than one match - the dotted name matching both a route and a view is the fixture's real example, not a contrived one - is recorded in `unresolved` with every candidate, never guessed.

**The `docs` tool** (`Mapin\Query\Query::docs()`, `DocsTool`/`DocsToolAdapter`, registered on both `MapinServer` and `mapin:query`) is two directions of the same `documents` edge read off whichever side the given key turns out to be: `documented_by` for a class/route/view/table/file key, `documents` for a section key - section 6.1's own phrasing ("sections that document the node, and nodes documented by a section") taken literally rather than expanded into something larger. `impact` also picks up a `docs` bucket now (`documents` added to `REVERSE_IMPACT_EDGE_TYPES`, a `section` -> `docs` grouping case) - section 6.1's own table already promised impact would group "methods, classes, routes, views, jobs, commands, docs" together, a promise with no data behind the last one until this phase; closing it is completing something already specified, not new scope.

**`mapin:docs`** is a real, disclosed design call, not an oversight: since Markdown already builds through `mapin:build`, this command currently delegates to it (`callSilently`, not `call` - a real bug caught writing it, not hypothetical: `call()` mixes the nested build's own human-readable report into the outer command's output stream, which broke `--json`'s promise of one parseable object the first time this was tested) and reports a docs-specific slice of the result (`SqliteStore::docsCounts()` / `Query::docsSummary()` - CLI sugar, not a section 6.1 tool in its own right, the same relationship `mapin:doctor` has to `stats`). Today it is functionally "`mapin:build` plus a docs-shaped report," stated plainly rather than dressed up as more - it earns a real, distinct reason to exist once phase 6 adds `--with-llm`, a network-touching flag `mapin:build` itself must never grow.

**Fixture and verification.** Extended the same canonical invented fixture GoldenTest.php already snapshots (`tests/Fixtures/docs/architecture.md`, `models.md`) rather than a separate one, per section 12/531's own instruction that one fixture app carries every pattern. Covers, each with its own named test in `tests/Feature/Phase4Test.php` rather than relying on the golden diff alone to say what changed and why (following 1.8's own precedent): a backticked FQCN mention, a plain short-class-name mention, a backticked route name, a plain route URI (resolved straight off the plain-text regex, not backticks), a backticked file path, backticked mentions of both an overridden and a conventionally-named table, a same-file anchor link (a genuine self-loop `links_doc` edge - `doc:docs/architecture.md` linking to itself - correct output, not a bug), a cross-file link exercised in both directions, and a real ambiguous short name: `dashboard.index` is already both a route name and a view name in the existing fixture, so the unresolved-with-candidates path is exercised without inventing an artificial class collision. Also covers the `docs` tool both ways, `impact`'s new `docs` bucket, and `mapin:docs`'s own summary output.

Golden snapshot regenerated deliberately and reviewed by hand, not trusted blind: 8 new nodes (2 `doc`, 6 `section`), 6 new `declares`, 9 new `documents`, 3 new `links_doc`, nothing removed or changed anywhere else in the graph - the module is purely additive, exactly as section 7's "separate extractor, same graph" framing describes. 56 tests total (42 carried over, 14 new), PHPStan level 6 and Pint clean.

**One thing carried forward, not introduced here:** only `PhpExtractor` creates a generic `file` node; `BladeExtractor` already emitted a `declares` edge from a Blade file's own `file:` key that silently drops for exactly this reason, and `MarkdownExtractor::edges()` emits the same-shaped edge for a `.md` file's own `file:` key, which drops the same way - not fixed, not worsened, disclosed once here rather than left to be rediscovered as if new.

**Not yet done, tracked rather than skipped:** the semantic layer (phase 6) does not exist, so `--with-llm` is not an accepted flag anywhere yet - deliberately, rather than accepting a flag with nothing behind it. `docs` reports only direct `documents` edges, no transitive discovery. And - matching exactly how phases 2 and 3 each called out "verified against the fixture" and "verified against a real application" as two separate, sequential steps - this phase has only done the first: the Markdown module has not yet been run against a real application's real documentation, only Testbench's invented fixture.

---

### 1.11 Phase 6 results (semantic layer), phase 5 deliberately skipped for now

Built the same day at Brandon's explicit request ("sigamos" after choosing to test locally against Ollama rather than the community-detection phase 5), which was itself preceded by a real infra decision: Celeron already runs Ollama with `phi3:mini`, `llama3.2:3b` and `nomic-embed-text` installed, confirmed live (`systemctl is-active` → active, listening on `*:11434`, answered a real `curl` in under a second) rather than assumed from a half-remembered model name ("creo que uno era phb3mini"). Brandon chose bringing a model to the notebook instead of pointing at Celeron, specifically to avoid loading a machine that already does other real work. That surfaced a real resource check before any code: the notebook was down to 324MB free RAM with the dev stack running - not something to add local LLM inference on top of - resolved by closing unrelated background work, not by proceeding anyway, which left 6.8GB free before continuing. `docker-compose.ollama.yml` (new, separate from `docker-compose.notebook.yml` - no `CODE_DIR` dependency, its own `vendor-ollama-dev` container on the shared `notebook-dev-network` so `vendor-app-dev` reaches it at `http://ollama:11434`) with `phi3:mini` pulled (2.2GB) and confirmed answering real prompts before any Mapin code was written.

**`Mapin\Extract\Markdown\Semantic\LlmClient`** is the one-method contract SPEC.md section 7 names: `extractConcepts(string $sectionText): ConceptExtractionResult` plus `modelIdentifier(): string` (needed for both the `source: llm` + model-identifier storage requirement and, as it turned out, for correct cache invalidation - see below). `NullClient` is the config default and the only implementation the container ever sees unless `config('mapin.llm.driver')` is explicitly set to `ollama` - two independent reasons nothing is ever sent anywhere by accident: `LlmConceptExtractor` is never even instantiated except from `DocsCommand`'s own `--with-llm` branch, and even if it were, the default driver is a no-op. `Drivers\OllamaClient` is the one shipped provider driver (SPEC.md section 7's "one provider driver"), built against Ollama's real `/api/generate` endpoint on a live, locally running v0.33.3 server, using `format: "json"` to constrain output to the `{"concepts": [...], "relations": [[...]]}` shape it parses - plain `curl`, no new Composer dependency, since a provider driver is optional pluggable surface, not core.

**`LlmConceptExtractor`** (input: "sections", per SPEC.md section 4's own extractor table) never runs as part of `mapin:build` - it re-reads every `.md` file already known to the graph directly off disk via `SqliteStore::docNodes()` (itself only ever populated by `MarkdownExtractor`), which makes "never sends PHP source to an LLM" a structural fact, not a discipline: there is no code path in this class that ever opens a `.php` file. Per-section caching by content hash, the same shape the deterministic build already uses at the file level, applied one level down: every section is hash-compared on every run, and only a changed or never-processed section calls the client. Two new `SqliteStore` methods make the store side of this work without disturbing anything else: `mergeNodeMeta()` merges into a node's existing meta rather than replacing it (unlike `upsertNodes()`'s blind overwrite), so the `llm_hash` this extractor stamps on a section node can never be clobbered by `MarkdownExtractor` re-writing that same node's `anchor`/`level` fields on some later, unrelated build; `clearSectionConceptEdges()` deletes a section's own `mentions`/`relates_concept` edges before regenerating them, keyed by `(file_id, line)` - the section's own heading line, the exact same attribution `MarkdownExtractor` already uses to scope `documents`/`links_doc` edges to one section, reused rather than inventing a second convention.

Two real problems surfaced building and verifying this, one caught before shipping and one only findable by actually running it against a real model:

1. **The cache key was wrong before any test ran against it.** The first version hashed only the section's own text. Designing the cache-invalidation tests surfaced the real failure mode this misses: a section already processed once by the default `NullClient` (nothing configured yet, or deliberately testing with `--with-llm` before a real driver exists) would read as "already handled" the moment a real driver got configured later, since the text never changed - silently producing nothing forever instead of actually running the configured model. Fixed by hashing `modelIdentifier() . '|' . $sectionText` instead of the text alone, so switching provider or model is treated exactly like the text changing. Covered by a dedicated test (`it('regenerating with a different model invalidates the cache instead of reading it as already handled')`) that fails without the fix.
2. **`temperature: 0` made a real model hang mid-response, not this driver's parsing.** Manually verifying `OllamaClient` against the actual local server (not the automated suite, which never touches a real network - see below) with `temperature: 0` set for reproducibility produced a syntactically complete, valid JSON answer followed by hundreds of trailing newline characters and `"done": false` - Ollama's own signal that generation had not actually finished. `json_decode()` correctly rejected the malformed result and the driver correctly fell back to reporting zero concepts for that section - the parsing and error-handling logic worked exactly as designed against what it was actually given; the bug was in asking for greedy decoding at all. Dropping the `temperature` option entirely (Ollama's own default sampling) produced a clean, complete response on every subsequent run. Determinism was never a promise this layer makes - SPEC.md's reproducibility guarantees are about the deterministic extractors - so it was not worth keeping at the cost of a driver that can hang on valid input from a real model.

**Verification.** `tests/Feature/Phase6Test.php` (7 tests) uses its own small, dedicated fixture (`tests/Fixtures/docs-semantic/notes.md`, two sections, no PHP at all - concepts are LLM-invented labels, not resolved code symbols, so nothing here needed the shared library-domain fixture, and nothing here touches or needs to regenerate the golden snapshot) and a `FakeLlmClient` test double (`tests/Support/FakeLlmClient.php`) for deterministic, network-free coverage of: concept/edge creation with correct `source`/`model` meta, a second run producing a full cache hit with zero further client calls, the model-identifier cache-invalidation fix above, reprocessing exactly the one section whose content changed while the other stays cached, and a client failure on one section producing a warning while the rest of the run still completes. One test drives the real `mapin:docs` command end to end: configuring `driver: ollama` pointed at an address nothing listens on (`http://127.0.0.1:1`) and asserting the command still succeeds normally without `--with-llm` - proof by actual behavior, not just code inspection, that a configured-but-unused driver is never touched. 63 tests total (56 carried over, 7 new), PHPStan level 6 and Pint clean.

Matching the project's own established split between the automated fixture suite and a real backend checked separately by hand (phases 2 and 3 both did this for their own real-world dependencies): `OllamaClient` and the full `LlmConceptExtractor` pipeline were both run for real against the actual local `vendor-ollama-dev` container, outside the test suite (a standalone script needing no Laravel boot at all, since none of this code depends on the framework being present). Fed the same two-section fixture through the real driver: correctly extracted `billing service`, `customer`, `credit`, `refunds` from the Refunds section and `calculator`, `shipping estimates` from Shipping, four `relates_concept` edges, every `mentions`/`relates_concept` edge meta correctly stamped `{"source":"llm","model":"ollama:phi3:mini"}`, and a second run against the same content reported a full cache hit (`processed=0, skipped=2`) with no further calls to Ollama - the caching mechanism verified against a real model's real output, not only against a fake one.

**Not yet done, tracked rather than skipped:** phase 5 (community detection, `hubs`/`communities`) remains untouched - phase 6 was built first at Brandon's explicit choice, not because 5 turned out to be unnecessary. `Query::docs()` was not extended to surface concepts or `mentions` edges - section 6.1's `docs` tool is specifically about the deterministic `documents` edge, and phase 6's own acceptance criteria (section 8's table) names no new query tool, so this stays a possible future addition rather than assumed scope. Only one provider driver exists (Ollama), matching "one provider driver" from that same acceptance row - nothing here generalizes to a second provider without writing one. Concept quality is a function of the configured model and prompt, not something this layer polices: `phi3:mini`'s real answers above include some overlapping concepts a stronger model might merge (`credit` and `customer` as separate ideas from what is arguably one relationship) - a small-model characteristic, not a driver defect. A concept node that stops being mentioned by anything after a section's content changes is never pruned - harmless clutter, not incorrect data, and not solved here. `--full` deletes the entire SQLite file before rebuilding, so it resets the semantic cache exactly the way it resets everything else - not a new limitation this phase introduces, just the existing behavior applying here too.

---

### 1.12 Phase 5 results (community detection and hubs)

Built at Brandon's explicit request ("vamos con la fase 5?"), out of numeric order - phase 6 (semantic docs) was built first, at Brandon's own earlier explicit choice (see 1.11), not because phase 5 turned out to be unnecessary.

**`Mapin\Analysis\WeightedGraph`** is a plain undirected weighted graph over integer node ids, with no dependency on `SqliteStore` or any Mapin-specific concept - Louvain itself is unit-testable against a hand-built graph with a known correct answer, independent of how a real codebase graph gets projected into one. Self-loops are tracked separately from ordinary edges rather than as a same-id adjacency entry, because Louvain's own multi-level aggregation needs them: collapsing a community into one node folds every edge that used to be internal to that community into a self-loop on the resulting meta-node, and the next level's modularity computation has to count that correctly (a self-loop contributes its full weight once to the graph's total weight but twice to its own node's degree - the standard convention) or every level after the first would silently be wrong.

**`Mapin\Analysis\Louvain`** is the standard two-phase algorithm (Blondel et al. 2008): local moving (each node joins whichever neighbouring community gives the best modularity gain, evaluated with the well-known simplified gain formula, or stays put) until a full pass moves nothing, then aggregation, repeated on the smaller graph until it stops shrinking. Deterministic by construction, not by convention: node iteration is always ascending id order, and a tied modularity gain always prefers the lower community id, with an epsilon guard against floating-point noise registering as a real difference - SPEC.md section 14's own gate for this phase ("same graph, same result across runs") holds regardless of PHP's own array-iteration or floating-point evaluation order, neither of which is otherwise guaranteed stable.

**`Mapin\Analysis\GraphProjection`** builds the projection section 8 describes, reused as-is by the `hubs` tool so its degree always means the same thing `mapin:communities` itself computed against. Node inclusion is an allowlist (`class`, `method`, `route`, `view`, `function` - cross-checked against every row of section 3.2's own edge table for which node types can ever be an endpoint of the nine tracked edge types), not a denylist - the real bug this phase's real-app check found, covered below.

**`Mapin\Analysis\CommunityDetector`** orchestrates: project the graph, pull nodes at or above the configured degree percentile (`config('mapin.communities.hub_percentile')`, defaulted to the top 1% section 16 itself proposed - closing that open question rather than leaving it unset) out before Louvain ever sees them, run Louvain on what remains, then reattach every excluded hub to whichever neighbour (from its real, full edge set, not the reduced graph) it shares the most weight with - processing hubs in a fixed order so an already-reattached hub can serve as another hub's own strongest neighbour too. Per-node membership is written to `nodes.meta.community` (as the community's representative node's own key, not an opaque internal id) via `mergeNodeMeta()` - the exact method phase 6 introduced for stamping a section's `llm_hash` without disturbing other meta a different extractor already wrote, reused here for the identical reason. A community-level summary (size, top five members by degree, dominant namespaces among class members) is written to the generic key-value `meta` table (`SqliteStore::setMeta()`/`getMeta()`, new - existing so far only for `schema_version`), since a community's own aggregate stats are not naturally one node's fact the way membership is.

**`hubs`** (`Query::hubs()`, `HubsTool`) reports degree always and `bridges` (SPEC.md section 8's "betweenness-lite": distinct neighbouring communities) only once `mapin:communities` has run - `null`, not zero, before then, so a caller can tell "not computed" apart from "genuinely bridges nothing". **`communities`** (`Query::communities()`, `CommunitiesTool`) reports the stored summary, or `found: false` before `mapin:communities` has ever run - per-node membership is not duplicated here, since it already surfaces through `node()`'s own generic `meta.community` field for free. Section 6.1's own table lists a `resolution?` argument for the `communities` tool; resolution is a Louvain parameter `mapin:communities` itself already takes (`--resolution=1.0`), and this tool only ever reads what that command last computed, so a resolution argument here would do nothing - treated as the same class of copy-paste drift already found and corrected once in section 3.1's node key table, not implemented as a dead parameter for the sake of matching the row literally.

**Two real bugs, both found only by running this against a real application, not by the automated fixture suite:**

1. **The first node filter was a denylist, and a ~20-node invented fixture could never have exposed how wrong that was.** The original `GraphProjection` excluded only `external` nodes and vendor-backed classes, which let every `table`, `middleware`, `doc`, `section`, `concept`, `component` and `file` node into the graph as a permanently isolated singleton - none of their own edge types (`maps_table`, `uses_middleware`, `documents`, `mentions`, `uses_component`, `declares`, ...) are among the nine SPEC.md section 8 actually tracks, so they were structurally incapable of ever merging with anything. Against the fixture, this just added a handful of harmless extra singletons, invisible to any of Phase5Test.php's assertions. Against the real application, it inflated the community count from a plausible 2,733 to a nonsensical 17,130 - roughly 10,000 of those were literally Markdown `section` nodes alone, the Markdown module's own byproduct nodes polluting an analysis that has nothing to do with documentation. Fixed by replacing the denylist with the allowlist described above.
2. **Fixing the filter did not retroactively clean up what the buggy version had already written.** `mergeNodeMeta()` only ever adds or updates a key, never removes one nothing in a given run touches - so the roughly 14,000 stale `meta.community` values the first (buggy) real run had already written to now-ineligible nodes survived the fix untouched, and kept showing up in `communityMembership()` right alongside the newly-correct ones. Found immediately after fixing bug 1, verifying against the same real graph a second time. Fixed with a new `SqliteStore::clearCommunityMeta()`, called at the start of every `CommunityDetector::write()` before writing fresh assignments, plus a dedicated regression test that reproduces the exact stale-state scenario by hand (no need for the old buggy code to still exist to prove the fix works).

**Verification.** `tests/Unit/LouvainTest.php` (7 tests): the textbook two-triangles-joined-by-one-bridge case splits correctly, determinism across repeated runs on the same graph, disconnected components each get their own community, an isolated node gets its own, relabeling anchors every community at its own lowest member id, and `WeightedGraph`'s own self-loop and repeated-edge-weight arithmetic. `tests/Unit/CommunityDetectorTest.php` (5 tests, store-level, no app boot): a seven-node hand-built graph with one deliberately unambiguous hub (weight-2 edge to one specific neighbour, so reattachment has exactly one correct answer regardless of SQL row order or PHP array iteration order) correctly excludes it, splits the two triangles it was bridging, and reattaches it to the right one; external and vendor-indexed nodes never get a community; two runs on the same graph produce identical membership and an identical summary; the summary itself reports size, top nodes and dominant namespaces correctly; and the stale-metadata regression test above. `tests/Feature/Phase5Test.php` (8 tests) exercises the real command/tool/MCP layer against the shared fixture - mechanical correctness only (a run completes and is deterministic, external/vendor nodes stay unassigned, `node()` surfaces membership through its own generic meta, `hubs`/`communities` behave correctly both before and after `mapin:communities` has run, `--json`/`--resolution` work, `mapin:query` drives the identical tool instances) - deliberately not claiming the fixture's own communities "align with known modules", since section 14's own acceptance gate for this phase is inherently a manual-review claim a 20-node toy app cannot make either way. 83 tests total (70 carried over, 13 new), PHPStan level 6 and Pint clean.

**Real-application verification**, matching this project's own established pattern (phases 2, 3 and 4 each did the identical fixture-then-real-app split): both fixes verified against the real application from section 4.2's benchmark (read-only against its own source; every write scoped to Mapin's own gitignored `storage/mapin/graph.sqlite`, its rebuildable artifact). After both fixes: 9,426 eligible nodes (down from the bogus 23,375), 2,733 communities (down from 17,130), 98 hub nodes excluded and reattached, computed in under a second. Manually reviewing the largest communities by size: each one's dominant namespaces clearly correspond to a single recognizable functional area of the real application (without naming real classes here, consistent with this document's own security discipline - see the GitHub-publish audit in the project's history) - a reporting module, a cash/billing module, a point-of-sale variant module, each appearing as its own distinct, correctly-separated community rather than one undifferentiated blob. The single highest-degree hub was the application's core transaction-record model, exactly the node real domain knowledge would predict; the highest bridge count of anything belonged not to that model but to the application's shared page layout template - correct and expected, since a template nearly every view in the app extends is definitionally the node that touches the most distinct communities, not necessarily the one with the most raw connections. Determinism was re-verified at this real scale too, not just in the unit tests: a full diff of every node's community assignment across two separate `mapin:communities` runs on the unchanged graph came back identical.

**Not yet done, tracked rather than skipped:** the fixture-based automated suite cannot and does not claim "aligns with known modules" - that is inherently a manual-review judgment, now made once against real data (above) rather than never made at all, but not something a future code change automatically re-verifies without doing the same real-app check again. Phase 7 (`JsExtractor`, `requests` edges) does not exist, so JS-originated calls are invisible to `impact`, `hubs` and `communities` alike - unchanged from every earlier phase's own disclosure of this. Community detection is manually triggered (`mapin:communities`) and never runs as part of a plain build, exactly matching section 8's own framing ("runs on the stored graph, never during extraction") - not a limitation, the documented design.

---

### 1.13 Phase 7 results (JS extraction)

Built at Brandon's explicit request ("fase 7 (JS)"), the phase 6 acceptance criteria closed the same day and the last remaining phase before release polish (phase 8).

**`Mapin\Extract\Js\JsExtractor`** matches section 1.3's own non-goal precisely: "Phase 7 maps HTTP calls in JS files to routes by URL; component trees and state flow are out of scope." Regex based, not a real JS/TS/JSX parser - the identical "good enough, not a full parser" call `BladeExtractor` already made for Blade directives, applied to a narrower surface still: `fetch(...)`, `axios.<verb>(...)`, `axios({...})`/`axios.request({...})`, `$.ajax({...})` and jQuery's `$.get`/`$.post` shorthand, matching only a literal string URL - a computed, templated or interpolated one is invisible on purpose, the same "no runtime-computed targets" non-goal every other extractor already respects. `.js`, `.jsx`, `.ts` and `.tsx` all discover under a shared `js` lang tag (`FileDiscovery`); `resources/js` joins the default `paths` list (Vite/Mix's own conventional source location), the identical kind of disclosed scope decision phase 4 made adding `docs`. Not an `Extractor` implementation, split into `nodes()`/`edges()` like `MarkdownExtractor`, for the same store-access reason: resolving a call whose verb does not match any route needs `SqliteStore::findRoutesByUri()`, which nothing in the plain `Extractor` interface's signature can reach. Wired into `BuildRunner`'s existing per-lang branching (the same shape phase 4 added for `'md'`, now joined by `'js'`) rather than a third pipeline. `requests` joins `impact`'s reverse edge types, grouped under a new `js_files` bucket (a `file` node, the only node type a `requests` edge's source is ever shaped as).

**Two real problems, both found only by running this against the real application - not the automated fixture suite, which is too small to reproduce either:**

1. **A `--full` rebuild of the real application now crashes on 512MB and needs several minutes even at 2GB - unrelated to this phase, but discovered while trying to verify it.** Reproduced identically with `resources/js` explicitly excluded from `--paths`, ruling out anything JS-related before looking further; the crash is `nikic/php-parser` exhausting memory mid-parse of an ordinary PHP file. The real application has grown from the 3,596 files phase 1's original benchmark measured to 4,657 now, with total nodes and edges roughly tripling (12,641 to 38,532 nodes at the time of this check) - not a Mapin defect, a real, disclosable finding that this application's own current size has outgrown the memory/time assumptions that benchmark was run under. Not fixed here (out of scope for JS extraction specifically), but worth flagging plainly rather than working around it silently: a `--full` rebuild on this application now needs a raised `memory_limit` and several minutes, not the original 33-43 seconds at 512MB.
2. **The first version matched every call by URL alone, discarding a verb an explicit `axios.<verb>(...)` call already states with full confidence.** Checked against the real application (read-only; every write scoped to Mapin's own gitignored `storage/mapin/graph.sqlite`): the single largest source of "ambiguous" URLs turned out to be `axios.get('/admin/cart', ...)` and `axios.post('/admin/cart', ...)` as two deliberate, separate calls a few hundred lines apart in the same real POS component - not vague or interpolated code, exactly the shape this extractor exists to resolve. `axios.<verb>(...)` names the verb as directly and unambiguously as `new X()` names a class, not a heuristic to be distrusted, so matching by URL alone threw away real information for no reason. Fixed by trying the exact `(verb, URL)` route key first, falling back to URL-only matching (a clean single match, or a genuine ambiguity reported in `unresolved`) only when the detected verb does not resolve. A related, smaller fix found the same way: a bare `fetch(url, { headers: {...} })` with no `method` key, or an `axios`/`$.ajax` object-form call with no `method`/`type` key, now defaults the verb to GET - each library's own documented default, not a guess, the same standard section 3.3 already applies to a `catch` type guaranteed by `Throwable`.

**Verification.** `tests/Feature/Phase7Test.php` (9 tests) uses its own small fixture (`tests/Fixtures/js/api.js`), reusing the shared library fixture only for its routes (which `JsExtractor`'s edges need to already exist) - the JS fixture is copied separately, so this phase never touches or needs to regenerate the golden snapshot. Covers every call shape above, both directions of the verb-priority fix (an explicit verb resolving cleanly despite a shared URI, and the URL-only fallback still reporting a genuine ambiguity when neither registered verb is GET, so the new default cannot silently paper over it), the GET default itself, a literal URL matching no route at all (silently ignored, the identical "zero matches = not a reference" rule the Markdown module already follows), and `impact`'s new `js_files` bucket. `mapinEdgeExists()`, previously declared inside `Phase4Test.php` alone, moved to `tests/Pest.php` alongside `mapinCopyPhase2Fixtures()` while writing this phase's own tests - the identical latent fragility that function's own docblock already warned against (a global function declared inside one test file is only defined when that specific file is required, which running a single other file by path does not guarantee), simply never hit until a second phase needed the same helper. 92 tests total (83 carried over, 9 new), PHPStan level 6 and Pint clean.

Re-verified against the real application after both fixes (forcing only the 101 JS files to be re-seen as changed - deleting their `files` rows, which `ON DELETE CASCADE` also clears their stale nodes/edges for, rather than another multi-minute `--full` rebuild unrelated to what changed): 101 `requests` edges, zero `requests` rows left in `unresolved` - every real call in the application resolved correctly. `impact` on a real controller method reached by a route a real JS component requests correctly listed that component under `js_files` at the expected depth, end to end through the real command, not just the extractor in isolation.

**Not yet done, tracked rather than skipped:** component trees and state flow remain explicitly out of scope, per section 1.3's own non-goal - this was never attempted. A URL built from string concatenation or template-literal interpolation is invisible, matching every other extractor's "no runtime-computed targets" non-goal - not a JS-specific gap. The real application's own `--full`-rebuild memory/time regression (finding 1 above) is disclosed, not fixed - it belongs to `PhpExtractor`/`Resolver`'s own memory profile at the application's current size, not to this phase's actual scope.

---

### 1.14 Phase 8 results (release polish)

Built at Brandon's explicit request ("fase 8 (release)"), the last phase - documentation, an extractor-pluggability gap found while writing that documentation and fixed rather than just disclosed, CI actually made to pass rather than assumed to, and a real fresh-install verification on both supported major Laravel versions. Tagging `v0.1.0`, publishing to Packagist, and any CI secret change are explicitly not part of this section's own work - section 13's own "ask first" list, honoured by stopping short of them here rather than doing them and asking forgiveness.

**CI had never actually been run since the very first commit.** Discovered by checking `gh run list` after pushing phase 7's commit and finding the first commit's own workflow run had already failed, three-plus hours earlier, unnoticed - the workflow file existing was never evidence it worked, exactly the same "verify, don't assume" discipline every other phase's real-app check already applies, just aimed at CI instead of a real Laravel application this time. Five real, distinct problems, each reproduced locally first (an isolated `/tmp` copy, never the real application), then fixed, then confirmed green on the actual GitHub Actions run rather than by re-reading the YAML:

1. `pestphp/pest-plugin-laravel` was required but never used (confirmed by grep for its own namespace across the whole test suite) and its own release matrix cannot simultaneously span Laravel 11, 12 and 13 once problem 3 below is in the mix - removed outright.
2. Testbench's own bundled skeleton views (`welcome.blade.php`, `errors/*.blade.php`) leaked into a genuinely fresh install's golden snapshot - invisible in a long-lived local dev container that had never run the test suite from a truly clean state, only visible in CI's own fresh checkout. Fixed via `config('mapin.exclude')`, set in `TestCase::defineEnvironment()`.
3. Every current Laravel 11.x patch release is flagged by a Packagist security advisory, and Composer's own install-time `policy.advisories.block` (a different, newer mechanism than `composer audit`) refuses to resolve any of them as a result - reproduced identically against a genuinely fresh `laravel/laravel` skeleton, confirming this is a real, current, widespread condition affecting any Laravel 11 install right now, not something specific to this package's own dependencies. Scoped around with `COMPOSER_NO_BLOCKING: 1` in the workflow's own `env:` block.
4. No release of `orchestra/testbench` supports Laravel 13 at all, confirmed from the actual Composer error listing every testbench version's own `laravel/framework` constraint, none reaching `13.*` - a genuine, currently unfixable external limitation, not a bug here. Handled with `continue-on-error: ${{ matrix.laravel == '13.*' }}`, chosen deliberately over excluding that matrix leg outright so the expected failure stays visible in every run rather than silently disappearing.
5. The committed golden snapshot itself included two Laravel-version-conditional default routes (`storage/{path}` GET and PUT) not present in every supported Laravel version - fixed with an allowlist filter in `GoldenTest.php`'s own snapshot function, keeping only routes whose `meta.action` starts with `Fixture\`, and excluding any edge touching a route the filter drops.

**The extractor-pluggability gap:** about to document the `Extractor` interface in `docs/EXTENDING.md`, and deliberately checking section 4's and section 11's own promise ("extractors are registered through the service provider and can be added by the host app or by third-party packages" / "keep extractors behind the `Extractor` interface so host apps and plugins can add their own") against the real code before writing it down, rather than assuming a promise this document itself already made had actually been kept. It had not: `BuildRunner::run()` hardcoded exactly `[$phpExtractor, new BladeExtractor]`, with no registration path at all. Fixed properly, not just disclosed as a gap the way the JS/Markdown limitation below is:
- `config('mapin.extractors')`, a list of FQCNs implementing `Extractor`, resolved through the container in `BuildCommand::handle()` and passed into a new `array $extraExtractors = []` parameter on `BuildRunner::run()`.
- A second, deeper problem surfaced while designing how to actually test the first fix rather than just claim it: `extractWith()`'s original "first supporting extractor wins" short-circuit meant a plugin extractor also supporting `.php` files could never get a turn at all, since `PhpExtractor` would always claim `.php` files first. Rewritten from first-match-wins to accumulate-and-merge - every extractor whose `supports()` returns true for a file contributes its own nodes, edges and warnings, all merged into one `Fragment` - unchanged behaviour for the two built-in extractors, whose own `supports()` checks are mutually exclusive by language anyway, so at most one of them ever matched a given file regardless.
- Verified end to end, not just unit-tested in isolation: `tests/Support/PluginTagExtractor.php`, a minimal test-only `Extractor` registered solely through `config(['mapin.extractors' => [...]])` inside one test, proves the real wiring (`BuildCommand` resolving config through the container, `BuildRunner` actually invoking it) rather than that the interface can merely be implemented. The test asserts both the plugin's own `external:plugin-tag:...` node and `PhpExtractor`'s own completely ordinary `class:Fixture\Services\TenantContext` node exist after one build - the plugin adds to core extraction, it does not replace it.
- `RouteExtractor`, `BindingExtractor` and `ScheduleExtractor` (whole-router/container/kernel input, not per-file) and `MarkdownExtractor`/`JsExtractor` (need store access to resolve a reference against the rest of the graph, hence their own `nodes()`/`edges()` split) remain genuinely unreachable through `config('mapin.extractors')` - `docs/EXTENDING.md` says so plainly rather than implying the gap above closed all of it. No `ToolSpec` (query/MCP tool) plugin mechanism exists either, and grepping this document's own history confirms none was ever promised - `docs/EXTENDING.md` does not claim one, and points out the existing generic tools already reach any node type by its own key convention without needing a dedicated tool.

**Documentation written:** `README.md` and `README.es.md` (why, requirements, install, quick start, MCP client setup, what it builds, the boundaries this package holds itself to, matching section 13's own Never list), `CONTRIBUTING.md` (setup, checks, code style, and - explicitly - why fixture-only tests are not the whole story, naming the real bugs this phase and phases 5/7 alike found only against a real application), `CHANGELOG.md` (Keep a Changelog format, one `[Unreleased]` section), `docs/EXTENDING.md` (above). `docs/MCP.md` itself was found to be stale while cross-checking it against `README.md`'s own MCP section - its tools table still claimed `hubs`, `communities` and `docs` "are not registered yet", true when first written, false since phase 5 and phase 6 landed; corrected.

**Real fresh-install verification, the phase's own acceptance gate ("fresh install on a clean Laravel 11 and 13 app works from the README alone") - done literally, not assumed.** A genuinely fresh `composer create-project laravel/laravel ... "^11.0"` and a second, separate `"^13.0"` skeleton, each in its own throwaway directory inside `vendor-app-dev`, each requiring this package only through a Composer path repository pointing at the real local checkout (`"options": {"symlink": false}`, a real file copy rather than a symlink, for realism) - the only concession to this package not being tagged and published yet, and the reason `dev-main` rather than a version constraint had to be named explicitly. Two real, install-blocking findings, both applying identically to Laravel 11 and Laravel 13 - neither is Laravel-version-specific:

1. **`laravel/mcp ^1.0.0-beta.1` has no stable release, and a fresh Laravel app's own default `minimum-stability: stable` rejects it outright** - independent of whether this package itself is tagged: even a real, stable `v0.1.0` git tag would satisfy Composer's own stability rules for *this* package, but the transitive `laravel/mcp` requirement would still fail under a consumer's default settings regardless. Not a temporary pre-tag artifact - a real, permanent requirement for every future installer until `laravel/mcp` itself ships a stable release. `README.md`/`README.es.md`'s Install sections were missing this entirely; fixed with an explicit `composer config minimum-stability beta` + `composer config prefer-stable true` step, explained as ongoing rather than a one-time setup quirk. This is the single most important finding of this phase - without it, the README as originally written would have failed for every real reader at the very first command.
2. **`mapin:doctor`'s `unresolved_ratio` check fails on both fresh skeletons** (0.622 on Laravel 11, 0.628 on Laravel 13, against a `< 0.5` threshold) - confirmed as expected, not a bug, by contrast with the real application's own `unresolved_ratio` of 0.122 on the same check: a skeleton with almost no first-party code has almost nothing for a call to resolve against, so the ratio is naturally high right after install and becomes a meaningful signal only once real application code exists. `README.md`/`README.es.md`'s Quick start sections now say so, so a first-time reader isn't alarmed by a failing health check before writing a single line of their own code.

With both applied, the full quick-start pipeline was run for real against each fresh skeleton, not assumed from the Laravel 11 result alone: `vendor:publish --tag=mapin-config` (config copied correctly), `mapin:build --full` (Laravel 11: 2,408 files, 4,992 nodes, 2,531 edges, 4,155 unresolved, 13.91s; Laravel 13: 2,668 files, 5,654 nodes, 2,926 edges, 4,919 unresolved, 17.19s - no crash, no anomaly, both comfortably fast against a trivial app), `mapin:find User --json` (found both the app's own `App\Models\User` and the framework's `Illuminate\Foundation\Auth\User`, correctly distinguished by `project: true/false`), and `mapin:mcp` (starts and exits cleanly on stdin EOF, confirming no boot-time fatal error, the appropriate bounded check for a command whose real job is to block on stdio forever). Both throwaway directories were deleted afterward; nothing under this verification touched the real application beyond its own gitignored `storage/mapin/graph.sqlite`, matching every other phase's own real-app discipline.

**Verification.** The existing 92-test suite (Pest, PHPStan level 6, Pint) re-run clean after every change in this phase, same as every prior phase - this phase's own new coverage is the CI matrix itself going green on the actual GitHub Actions run (not locally reproduced alone) and the two fresh-install runs above, neither of which fits naturally into the fixture-based automated suite (a fresh Composer install, by definition, cannot be exercised by a test running inside an already-installed package's own repository).

**Not yet done, deliberately, per section 13's own boundary:** `v0.1.0` is not tagged, this package is not published to Packagist, and no CI secret was touched - all three require asking Brandon first and getting a clear go-ahead, not something this phase does on its own initiative even though everything else it needed is now in place.

---

### 1.15 Query misses tracking (built 2026-09-08, post-release)

Built at Brandon's explicit request, after v0.1.1 shipped: he wants every session that queries the graph and comes up empty to leave a trace, so he and Lauti can periodically review what the graph couldn't answer and close real gaps in the extractor - not something either of them has to remember to write down by hand.

**`query_misses`** (section 5), a new table bumping schema to version 3. `Query::envelope()` (section 6) writes one row here on every `found: false` answer - the one place CLI and MCP already converge, so this covers all 14 tools without touching any of them individually. Getting the semantics right meant reading `Query.php` closely first, not assuming a uniform rule: a `found: false` in this codebase specifically means the *node/key/route being searched for* does not exist - `callers`/`callees`/`impact`/`docs`/`hubs`/`unresolved`/`stats` all treat an empty result on a real node as `found: true` (SPEC.md's own "never answer with an approximate match" discipline already required this distinction to exist; this feature only had to respect it, not invent it), and `path()` treats "both nodes exist, no route within `max_depth`" the same way. None of those legitimate empty results write a row here - confirmed by a dedicated test asserting a real method with zero real callers produces zero misses.

**`mapin:misses`** (section 9), the one deliberate exception to section 13's "never write outside `storage/`" - and only when a human runs it. Exports every unexported row to a JSONL file inside the *host* project (default `docs/mapin-misses.jsonl`), one line per miss, then marks those rows exported (`exported_at`) so a second run reports zero and never duplicates a line. This is also why the file lives in the host project's own repo, never inside this package's own `vendor/` directory: multiple teammates across different machines need to write to the *same* file and commit it together, which only works if it is versioned in *their* repository, not shipped as part of this one.

**The privacy question this raised, and how it's answered for good, not just for now.** Brandon asked directly, before any of this was built: if the capture mechanism lived inside this package's own code, would a third party who installs Mapin end up reporting their own project's route names and gaps back to us? The answer had to be an unambiguous no, and section 13 point 7 now says so as a first-class boundary, not an afterthought: `query_misses` never leaves the *local* `graph.sqlite` on its own, `mapin:misses` never makes a network call, and it only ever writes into the project that is running it - there is no path in this feature, today or by design, for one installation's misses to reach another party.

**Verification.** 5 new tests (2 unit, store-level, in `SqliteStoreTest.php` - recording a miss round-trips its JSON correctly, and `markQueryMissesExported` scopes to exact ids rather than a point in time; 3 feature, in the new `QueryMissesTest.php` - the empty-result-is-not-a-miss distinction above, `mapin:misses` exporting then reporting nothing new, and a second batch appending rather than overwriting). 101 tests total, PHPStan level 6 and Pint clean.

**Real-application verification**, same discipline as every other phase: schema bump against the real application forced the full rebuild the schema change always promises (292s, 25,754 nodes and 74,432 edges - both figures still matching `SqliteStore::counts()` exactly, confirming the 1.14 count-accuracy fix holds under this change too), then two real misses generated on purpose (`mapin:find` and `mapin:route` against names invented not to exist), exported for real to `docs/mapin-misses.jsonl` inside the real application's own repository, and a second `mapin:misses` run confirmed idempotent (`exported: 0`, file unchanged at 2 lines). The test file was deleted afterward and the real application's own `vendor/` restored to its normal Composer-managed state - nothing under this verification was left behind.

**Not yet done, tracked rather than skipped:** no size cap or rotation on `query_misses` or the exported JSONL - left as an open question for whenever it becomes a real problem, not solved speculatively. No configuration to disable the capture (SPEC.md's own "don't design for hypothetical future requirements" - nothing has asked for an opt-out yet, and the mechanism is purely local and low-cost by construction).

---

### 1.16 Automatic rebuild via git hooks (built 2026-09-08, closing a gap disclosed since phase 8)

Built at Brandon's explicit request ("avancemos con el hook"), closing the gap every phase since release has disclosed plainly: unlike graphify, this package never rebuilt itself automatically. graphify's own history (retired predecessor, section 13's own reasoning) is the direct precedent and the direct reason this is a *local*, per-developer hook rather than a shared one: graphify originally ran via a git hook too, until it started saturating a *shared* server's CPU and had to move to a post-deploy hook instead. That risk doesn't apply here - this package has no shared graph to begin with (`graph.sqlite` is local and gitignored per developer, SPEC.md section 13 point 2), so each developer's own git hook only ever spends their own machine's own resources, on their own graph.

**`mapin:install-hooks`** writes a `post-commit` and a `post-merge` hook that run `mapin:build` in the background (`(cd "$(git rev-parse --show-toplevel)" && <command> > /dev/null 2>&1 &)`) - backgrounded on purpose, so a commit or merge is never blocked waiting for a rebuild, even though the common case (an incremental build touching a handful of files) is already fast enough not to need that. `--command` is deliberately not hardcoded to `php artisan mapin:build`: a real host application may run entirely inside Docker (this project's own real verification target does), where the actual command is `docker exec <container> php artisan mapin:build` - assuming a bare host PHP would have been wrong for the one real application this was tested against.

**Two real design questions, resolved by reading how git itself behaves rather than assuming:**
1. **Never overwrite an existing hook wholesale.** A project may already have its own `post-commit`/`post-merge` for something unrelated. The installed block is wrapped in a unique marker comment (`# mapin:auto-build ... # mapin:auto-build end`) and only *that* block is replaced on reinstall (a `preg_replace` scoped to the markers) - everything else in the file, before or after, survives untouched. Confirmed with a dedicated test that plants a fake hook with unrelated content first.
2. **Where the git hooks directory actually is.** Not assumed to be `<base_path>/.git/hooks` - resolved via `git rev-parse --git-dir` run from `base_path()`, the same command git itself uses, so a project with a relocated or worktree-style `.git` still resolves correctly.

**Verification.** 5 new tests in `InstallHooksCommandTest.php`: hooks are created, executable, and contain both the marker and the exact `--command` given; running the install twice does not duplicate the block; an existing hook's own unrelated content survives a (re)install; reinstalling with a *different* `--command` replaces only mapin's own block, not content added around it; and a genuine non-repository fails with exit 1. That last test needed a real standalone temp directory rather than just deleting `.git` from Testbench's own skeleton app - that skeleton lives inside this package's own `vendor/`, itself inside this package's own real git repository, so git found the real repo by walking up the directory tree exactly as git is supposed to, the same way it would for any file genuinely nested inside a real project's repo. Not a bug in the command; a reminder that "delete the marker of the thing I'm testing" and "genuinely isolate the test" are not always the same action. 106 tests total, PHPStan level 6 and Pint clean.

**Real-application verification**, deliberately without leaving a throwaway commit in the real application's own history: hooks were installed for real (`--command="docker exec vendor-app-dev php artisan mapin:build"`, since the real target runs entirely inside Docker), a real file was touched, and the installed `post-commit` script was run directly (`sh .git/hooks/post-commit`) - the same thing git itself runs after a real commit, without needing to create one. It returned immediately (backgrounded, confirmed non-blocking) and the graph updated itself within a few seconds, confirmed by `mapin:stats`'s own `last_build.finished_at` moving forward with no command run by hand. The touched file was reverted, `vendor/` was restored to its normal Composer-managed state, and the hooks themselves were left installed in the real repository, since that is the actual point of this feature - the first hook is now the same one Brandon works under day to day, not a demo.

---

### 1.17 Vendor class methods surviving an incremental build (built 2026-09-08, the real cause behind a slice of the unresolved audit)

Started as an audit of the ~11,000 unresolved `calls` rows against the real application - Brandon wanted to know whether `BladeExtractor`'s own regex-not-a-parser gap was worth closing before anything else. It wasn't (zero of those rows came from a `.blade.php` file - confirmed by joining `unresolved` against `files` and grouping by extension), and the dominant pattern (variables losing their type through long Eloquent/query-builder chains) is the same non-goal this document already accepted since the spike. But one real, sizeable pattern stood out: 371 of those rows (about 3.4% of all unresolved calls) began with `redirect()` or `back()`. That number turned into a much larger finding once actually chased down, not assumed.

**First hypothesis, disproven by reading Laravel's own real source before writing anything.** The obvious guess - "Mapin doesn't know what `redirect()`/`back()` return" - is wrong. `redirect()` has a native union return type (`Redirector|RedirectResponse`), and `TypeExpr::fromNode()` already takes the first union member; `Redirector::back()`/`to()`/`route()` have a plain, unconditional `@return \Illuminate\Http\RedirectResponse` docblock, exactly the simple case `TypeExpr::extractReturnExpr()`/`fromDoc()` already handles. Confirmed empirically before touching any code: a fresh `mapin:build --full` resolved a real `redirect()->back()->withInput()` chain perfectly, edge for edge, no special case needed.

**The real bug, found by comparing that same chain across a full build and the very next incremental one.** `ClassMeta->methods` (return types included) is extracted correctly for every class, vendor or project - `DeclarationVisitor::visitMethod()` has no `isProject` gate. But `ClassMeta::toMeta()` (what actually gets written to `nodes.meta`) never serialized `methods`, for any class. The only way `SqliteStore::hydrateIndex()` can rebuild `->methods` on an incremental build (one that doesn't re-parse a given file) is from that class's own `method:` nodes - and those are only ever created for `project: true` classes (`PhpExtractor::buildClass()`'s own `if ($class->project)` gate, a deliberate choice from phase 1 so the graph isn't saturated with thousands of Illuminate methods). A vendor file only gets re-parsed when its content hash changes (rare - essentially only `composer update`) or on `--full`. The practical consequence: after a project's first `--full`, every incremental build silently forgets a little more about what vendor classes can do - not because a type was ever genuinely unknown, but because it was known once, during whichever build last happened to touch that exact file, and then had no way to survive being remembered. This degrades monotonically with normal, correct use of the tool - the more incremental builds a project runs, the worse chained calls through vendor code resolve, with no file of the caller's own ever needing to change. Exactly the class of problem section 13's own "never leave a stale graph silently" boundary exists to catch, just not the shape anyone had gone looking for yet.

**Fix, kept to the one place this needed to change:** `ClassMeta::toMeta()` now includes `methods` in the persisted JSON, but only when `! $this->project` - project classes already get this back for free from their own `method:` nodes, so persisting it twice there would just be dead weight on every project class's own node. `ClassMeta::fromMeta()` loads it back when present. `SqliteStore::hydrateIndex()` needed no change at all: for project classes its own `method:`-node query still repopulates `->methods` from scratch exactly as before; for vendor classes that query now simply finds nothing to add, because there was never anything to add - the vendor class already arrived with its methods intact from `fromMeta()`.

**Verification.** A dedicated fixture (`tests/Fixtures/vendor-degradation/`, its own `FakeVendor\Redirector`/`RedirectResponse` with a docblock-only return, deliberately outside the shared golden fixture) and a new test, `VendorMethodPersistenceTest.php`, that does exactly what exposed the bug: full build, confirm the chain resolves; touch only the caller; incremental build; confirm it still resolves. Written to fail without the fix and confirmed it actually does - reverted the fix via a tagged git stash, watched the test fail at exactly the incremental-build assertion (not the full-build one), restored the fix, watched it pass again. 107 tests total, PHPStan level 6 and Pint clean.

**Real-application verification, twice, in two different real environments - not the same fixture-then-real-app split as usual, because the fix touches how vendor code is remembered across builds specifically, which no single fixture-scale project could exercise as convincingly as an actual full-size vendor tree.** First, against the real application directly (before its own working tree moved to other work mid-session): a full build resolved the real chain perfectly; the very next incremental one lost it, exactly as the bug predicted - not a fixture recreation, the actual failure, caught live. Second, once the fix was written, against a second real Laravel 13 application (a separate, deliberately-isolated pentesting lab environment, refreshed for this verification with a real `composer install` under PHP 8.3): full build, then an incremental build touching only one new file - `Illuminate\Routing\Redirector` was not re-parsed in that second build - and the chain still resolved end to end, edge for edge, zero unresolved for that file. Both throwaway files and directories were removed afterward, and the second environment's own `composer.json` was restored to depend on the published package rather than the local path used to test the unreleased fix.

**Not yet done, disclosed rather than assumed away:** the audit that started this only fully explains a slice of the ~11,000 unresolved calls - the `redirect()`/`back()` pattern specifically, and by extension any other vendor method chain a future incremental build might have been silently forgetting the same way. The bulk of the remainder is still the accepted non-goal (untyped parameters, long builder chains) this document has named since the spike, unchanged by this fix. No attempt was made to retroactively re-audit the full unresolved count against a graph rebuilt with this fix in place - that number will now naturally improve on the real application's own next `--full`, not something worth re-measuring by hand here.

---

### 1.18 `findNodes()` ranking a short-name search against the wrong column (found 2026-09-08, reported from real use)

Reported by a separate Claude session doing real work against this graph, not found by this project's own testing: `mapin:find "ServiceOrder"` answered `found: false` for a plain Eloquent model that genuinely exists, with the graph fully up to date. Brandon named it explicitly as the one to fix first, ahead of two other reports in the same message, because "es el caso de uso más básico de la herramienta" - a short-name search is the primary way section 6.1 itself documents `find()` being called, not an edge case.

**Confirmed the node was never actually missing** before looking at ranking logic at all - `class:App\Models\ServiceOrder` existed in `nodes` with id 6694. So the bug was somewhere between "the row exists" and "the query returns it," not in extraction.

**Root cause, found by reading `SqliteStore::findNodes()` next to `Query::exactMatches()` rather than assuming they agreed.** `exactMatches()` (1.17's neighbor in this same file, unrelated bug) correctly treats a key match, a full-name match, or a match on the segment after the last `\` as exact - but it can only rank rows `findNodes()` actually hands it, and that method's own `ORDER BY (nodes.key = :exact2) DESC` compares the search term against a node's *full* `key` (`"class:App\Models\ServiceOrder"`), which a bare short name like `"ServiceOrder"` can never equal. For every short-name search, that `ORDER BY` was a silent no-op - rows came back in whatever order SQLite's own query plan happened to produce, then got cut at `LIMIT 25`. Confirmed empirically, not assumed: a direct SQL query replicating `findNodes()`'s exact `WHERE`/`LIMIT` against the real application's graph returned 226 rows matching `LIKE '%ServiceOrder%'` (other classes it's a substring of, their methods, their files, route and doc mentions), and the real node was not among the first 25.

**Fix.** Two more `ORDER BY` terms, mirroring in SQL the same two extra rules `exactMatches()` already applies in PHP: `(nodes.name = :name2) DESC` (full-name match, e.g. a function or route key without a class prefix) and `(nodes.name LIKE :suffix) DESC` with `:suffix = '%\' . $name` (the node's own `name` ending in `\` + the search term - the short-class-name case that was the actual failure). This only changes which 25 rows survive the cut and their order among themselves - `exactMatches()` itself needed no change, it was already correct, just starved of the row it needed to see.

**Verification.** A new test in `SqliteStoreTest.php`: 30 decoy nodes each containing `"ServiceOrder"` as a substring (never an exact key or short-name match) plus the real node inserted last (highest rowid, the one a plain insertion-order `LIMIT 25` would drop first), asserting the real node survives. Deliberately calls `findNodes()` with no `$type` filter, matching how `Query::find()` actually calls it in the common case - an earlier version of this test passed a `NodeType` filter and, by coincidence, kept passing even with the bug still in place: filtering by type let SQLite walk the `nodes_type(type, name)` index in name order, and `"ServiceOrder"` (no numeric suffix) sorts before `"ServiceOrder0"..."ServiceOrder29"` lexicographically, masking the exact failure mode this fix targets. A test that passes for the wrong reason is worse than no test - re-verified by reverting the fix via a tagged git stash and confirming the corrected (no-`$type`) test fails at exactly the assertion it should, then restoring the fix and confirming it passes again. 108 tests total, PHPStan level 6 and Pint clean.

**Real-application verification.** The fixed `SqliteStore.php` was copied directly into the real application's installed `vendor/nexvia-solutions/mapin` (no `composer.json`/`composer.lock` changes, since this application depends on the published package rather than a local path repository) and `mapin:find "ServiceOrder"` was re-run against the same on-disk graph used to discover the bug (`files_changed_since_build: 0`, no rebuild needed - this is a query-layer fix, not an extraction one): `found: true`, `class:App\Models\ServiceOrder`, confidence 1.0. Afterward `vendor/nexvia-solutions/mapin` was deleted and reinstalled from scratch via `composer install` to confirm the real application's own runtime state was restored to exactly the published package, not left holding the unreleased fix.

---

### 1.19 The MCP server never completing a classic handshake (found 2026-09-08, reported from real use)

The second of three real bugs reported in the same message from a separate Claude session doing real work against `mapin:mcp` - the other two are 1.18 (fixed first, named explicitly as higher priority: "es el caso de uso más básico de la herramienta") and the `graph_staleness` hash-length mismatch (open). Reported symptom: the MCP connection never completes at all - not a wrong answer, no answer.

**Reproduced directly, not inferred from the report.** A raw `initialize` JSON-RPC request piped over stdio into `php artisan mapin:mcp` (bypassing any client entirely) came back:
```json
{"jsonrpc":"2.0","id":1,"error":{"code":-32601,"message":"The [initialize] handshake was removed in MCP 2026-07-28. Send the protocol version in the request [_meta] instead.","data":{"supported":["2026-07-28"]}}}
```

**Root cause: an upstream, deliberate BC break, not a Mapin bug.** `laravel/mcp` v1.0.0-beta.1 (Mapin's own pinned dependency, `composer.json`'s `^1.0.0-beta.1`) shipped [PR #296](https://github.com/laravel/mcp/pull/296), which drops the classic `initialize` handshake entirely in favor of MCP `2026-07-28`'s per-request `_meta.protocolVersion` - `Laravel\Mcp\Server::handle()` rejects any `initialize` call outright unless a handler has been explicitly registered for it (`! isset($this->methods['initialize'])`), and no built-in handler exists for it in this release; `ProtocolVersion::serverSupported()` also returns only the newest version by default. Confirmed this is not accidental: the same PR's own description names the exact escape hatch - "A server that still needs the old handshake can register it with `addMethod('initialize', ...)`; the rejection only fires when no handler is registered." Checked `dev-main` (unreleased, ahead of the tagged beta) too, in case this had already been addressed there - [PR #304](https://github.com/laravel/mcp/pull/304) does add legacy-era negotiation, but only to the package's own MCP *client* (`Laravel\Mcp\Client`, for a Laravel app calling *out* to other MCP servers) - the *server* role Mapin actually uses gained no such fallback.

**Registering a handler alone is not enough, found by reading `Server::handle()` past the first check rather than assuming the fix stops there.** Every request - not just `initialize` - goes through `validateProtocolMeta()` right after, which unconditionally demands `_meta.protocolVersion` and `_meta.clientCapabilities`. A classic client never sends `_meta` at all, on `initialize` or on anything after it: it negotiates the protocol version once, up front, and expects that to hold for the rest of the session - the entire point of the new per-request `_meta` mechanism is to replace that session-level assumption. A handler for `initialize` alone would pass that first message and then break on the very next one (`tools/list`, `tools/call`, ...).

**Fix, both pieces contained inside Mapin's own `MapinServer` - no vendor patching, no dependency downgrade (the user-facing alternative considered and declined: pinning to a pre-1.0 `laravel/mcp` release that still speaks classic `initialize` natively, at the cost of rewriting `MapinServer`/the fourteen `ToolAdapter` classes against a different, older API surface):**
1. `Mapin\Mcp\Methods\LegacyInitialize` (`Laravel\Mcp\Server\Contracts\Method`), registered via `MapinServer::boot()`'s `$this->addMethod('initialize', LegacyInitialize::class)`. Answers with `protocolVersion` (echoing the client's requested version when it's one `ProtocolVersion::initializeSupported()` actually lists, falling back to the newest of those otherwise - never inventing a version outside what the package itself claims to still understand), `capabilities`, `serverInfo`, and `instructions` - the classic `InitializeResult` shape, not the newer `server/discover` one `Laravel\Mcp\Server\Methods\Discover` already answers with for `2026-07-28` clients.
2. `MapinServer::validateProtocolMeta()` overridden to return immediately when `$request->meta()` is `null` - `_meta`'s outright *absence* is itself the signal a `2026-07-28`-aware client would never produce (that client always sends it, on every request, by construction), so no per-session state needs tracking to tell a legacy request from a modern one; a request that does carry `_meta` still goes through the full, unmodified `parent::validateProtocolMeta()` check, `ErrorCode::UNSUPPORTED_PROTOCOL_VERSION` included.

`notifications/initialized` (the classic handshake's own closing message) needed no handling at all: `Server::handle()` already returns immediately for any JSON-RPC message with no `id`, regardless of method name, before either check in this fix ever runs.

**Verification.** Three new tests in `McpLegacyHandshakeTest.php`, driving raw JSON-RPC through `MapinServer::handle()` via a minimal in-memory `Transport` fake (`MapinServer::tool()`, what every other MCP test in this suite uses, calls a tool directly and never exercises `handle()`/`validateProtocolMeta()` at all - the exact layer this bug lives in): a classic `initialize` now answers correctly; a full session (`initialize` → `notifications/initialized` → a `tools/list` carrying no `_meta`) completes end to end with a real tool list back; and a modern request whose `_meta.protocolVersion` names an unsupported version is still rejected exactly as before - confirming the relaxation is legacy-only; and does not weaken the strict path any client speaking `2026-07-28` still relies on. Reverted the fix via a tagged git stash first and confirmed the first two tests fail with exactly the original upstream error surfaced as a JSON-RPC `error` response (the third, modern-path test correctly keeps passing either way), then restored the fix and confirmed all three pass. 111 tests total, PHPStan level 6 and Pint clean.

**Real-application verification.** Both new files copied directly into the real application's installed `vendor/nexvia-solutions/mapin` (`composer dump-autoload -o` after, so the new `LegacyInitialize` class resolves), then a full three-message classic session piped over stdio into the real `php artisan mapin:mcp`: `initialize` answered with the negotiated version, server info, and instructions; the `initialized` notification produced no response, correctly; `tools/list` (no `_meta`) answered with the real application's full fourteen-tool list. A separate run confirmed the modern path is untouched: a `server/discover` carrying valid `_meta` succeeded, and a `tools/list` whose `_meta.protocolVersion` named an unsupported version was still rejected with the same `UNSUPPORTED_PROTOCOL_VERSION` error as before this fix. `vendor/nexvia-solutions/mapin` was deleted and reinstalled from scratch via `composer install` afterward, restoring the real application's runtime state to exactly the published package.

**Not yet done, disclosed rather than assumed away:** `ProtocolVersion::initializeSupported()` (the package's own list of versions the classic handshake still answers for) names only `2025-11-25` and `2025-06-18`; a client opening with an older classic version (`2025-03-26`, `2024-11-05`) still gets a version it didn't ask for in the response, matching how the package's own `Discover` method already treats version selection - not a gap this fix introduced. If `laravel/mcp` ever ships its own native backward-compatible server handshake in a later release, this shim becomes redundant, not wrong; nothing here assumes it is permanent.

---

### 1.20 `graph_staleness` comparing hashes of two different lengths (found 2026-09-08, reported from real use)

The third of three real bugs reported together in one message from a separate Claude session doing real work against Mapin - 1.18 (fixed first, "el caso de uso más básico") and 1.19 (fixed second, "nunca conecta") are the other two. Symptom: `mapin:doctor`'s `graph_staleness` check reported the graph stale immediately after a fresh, real build with zero files changed - not intermittent, every time.

**Root cause, found by reading where each side of the comparison actually comes from, not assuming they'd match.** `built_commit` (`Query::graphBlock()`, sourced from the `builds` table) is written by `BuildRunner::currentCommit()`, which deliberately truncates to `substr($head, 0, 12)` - a 12-character prefix. `head_commit` is `GitStatus::headCommit()`, a real `git rev-parse HEAD`, always the full 40-character SHA. `DoctorCommand`'s own staleness check (`$graph['built_commit'] !== $graph['head_commit']`) is a plain string comparison - a 12-character string can never equal a 40-character string, even when both name the exact same commit, so this check reported stale on literally every graph, including one built the same second the check ran.

**Fix.** One comparison, `DoctorCommand.php`: `! str_starts_with($graph['head_commit'], $graph['built_commit'])` in place of `!==`. A short git hash is by construction always a strict prefix of its own full hash, so this is correct regardless of which length `built_commit` happens to be - a 12-character prefix from an existing graph, or (hypothetically) a full hash, without needing to know or care which. Nothing about what gets stored changed; the fix is confined to how the two already-correct values get compared.

**Verification.** A real-git-repo test was needed, not an incidental one - `base_path()` in this project's own Feature tests has no `.git` of its own by default (established in `InstallHooksCommandTest.php`), so `built_commit`/`head_commit` both come back `null` and the existing `mapin:doctor` test (`Phase3Test.php`, present since phase 3) never actually exercised a real comparison between two real hashes at all: a first version of this regression test, written before noticing this, "passed" identically with and without the fix - both sides `null`, the buggy comparison never even reached. A dedicated test using the same `git init` inside `base_path()` pattern `InstallHooksCommandTest.php` already established (scoped to just this one test, torn down after, since every other test in that file relies on `base_path()` *not* being a repo) makes a real commit, runs a real `--full` build, and asserts `graph_staleness.ok` is `true` with `detail: 'current'`. Reverted the fix via a tagged git stash and confirmed the test now fails against real hashes (`built at cc50c1fcd929, HEAD is cc50c1fcd9297830e1c45e6f43815c849d6c8732 (0 files changed)` - the two values genuinely are the same commit, cc50c1fcd929 being the literal first 12 characters of the full hash), then restored the fix and confirmed it passes. 112 tests total, PHPStan level 6 and Pint clean.

**Real-application verification.** The fixed `DoctorCommand.php` copied directly into the real application's installed `vendor/nexvia-solutions/mapin`; `mapin:doctor --json` against the real, already-current graph now reports `"graph_staleness": {"ok": true, "detail": "current"}` (previously always `false` regardless of actual staleness). `vendor/nexvia-solutions/mapin` deleted and reinstalled via `composer install` afterward, restoring the real application's runtime state to exactly the published package.

**Not yet done:** this fix only touches the one place this comparison happens (`DoctorCommand`). Nothing else in the codebase compares `built_commit` against `head_commit` directly (confirmed by search) - every other consumer of the `graph` block (MCP clients, the CLI's `--json` output) already receives both raw values and is left to interpret them itself, which `filesChangedSince()`'s own `git diff --name-only <builtCommit>` already does correctly regardless of hash length, since git itself resolves an abbreviated SHA.

---

### 1.21 `BladeExtractor` becomes compiler-based, closing the gap 1.8 named (built 2026-09-08, at Brandon's explicit request)

Section 4's original design always described `BladeExtractor` as compiler-based - compile the template with the app's real `BladeCompiler`, run the same reference visitor every `.php` file already goes through over the compiled output - with the regex scanner as only the documented `--no-boot` fallback. Phase 2 (1.5) never built that: regex passed every case tested and was simpler, so the compiler path was skipped, and 1.6/1.8 both disclosed the gap that left open - `route()`/`view()` written as literal PHP inside a `.blade.php` file (`{{ }}`, `{!! !!}`, an `@php` block) was invisible, because nothing ever parsed that PHP. Earlier the same day, the audit that led to 1.17 asked a narrower question - "does BladeExtractor's regex gap explain any of the ~11,000 unresolved calls against a real application" - and answered no (zero of those rows came from a `.blade.php` file, because nothing Blade-related was ever parsed as PHP to *become* unresolved either). That answered a different question than "is there a real, disclosed gap here" - there is, in 1.8 - and Brandon asked directly to close it by building what section 4 always intended, not to patch the regex scanner further.

**Composition, not a full rewrite - and why.** The new `BladeExtractor` (`requiresBoot(): true`) keeps the regex scanner, renamed `DirectiveScanner`, as the authoritative source for structural edges (`Includes`, `UsesComponent`, `Declares`) and composes it internally; it *adds* compiling the template and resolving the compiled PHP through the same `Resolver`/`ReferenceVisitor` pipeline every `.php` file already uses, for what a regex scanner structurally cannot see: `route()`, `view()`, `app()`, method calls, `new`. Read all 1105 lines of `ReferenceVisitor` before deciding this, not assuming: Blade compiles `@include`/`@extends` to calls on the `$__env` runtime variable (`$__env->make(...)`, `$__env->startSection(...)`), never to `view()` - `ReferenceVisitor` is generic PHP-AST walking with zero Blade awareness, so getting the *same* structural edges from compiled output would mean teaching it `$__env`'s real type, a framework-runtime special case, not a generalizable improvement. Two sources, each authoritative for what it is actually good at.

**Mechanically, this works because `PhpExtractor`/`Resolver` already separate "parse" from "resolve" by design** (section 2): `PhpExtractor::extract()` caches a `NameResolver`-processed AST in memory, keyed by relative path, and `Resolver::resolveFile()` consumes whatever AST it is handed - no re-parse, no filesystem access, `$file` is a pure string label attached to every edge/unresolved row. `BladeExtractor::extract()` mirrors this exactly: resolve `Illuminate\View\Compilers\BladeCompiler` from `$ctx->container` (already the real, booted `Illuminate\Foundation\Application` by the time this runs - `BuildCommand.php` passes `$this->laravel` directly, and `blade.compiler` is a standard binding any booted app already has, so nothing new needed wiring into `ExtractionContext`), `compileString()` the source (the stateless method - not `compile($path)`, which writes disk cache files), parse the result, run just `NameResolver` (Blade output never declares classes/functions, so no `DeclarationVisitor`), and cache it behind its own `parsedStatementsFor()`. `BuildRunner`'s pass-2 resolve branch now picks whichever extractor's cache applies by `$file->lang`, and the extractor-construction line picks `BladeExtractor` or `DirectiveScanner` based on `$ctx->booted` - `requiresBoot()` itself is documentation only everywhere in this codebase (confirmed nothing reads it, including on `RouteExtractor`/`BindingExtractor`, which are gated by `ExtractionContext` null-checks instead), so this is the first place that flag's *intent* actually gets acted on.

**A real, blocking bug found while designing this, not after shipping it: `.blade.php` files never got a generic `file:` node.** Every `.php` file gets one from `PhpExtractor::buildFragment()`; a `.blade.php` file never has, so the existing `Declares` edge (file → view) `BladeExtractor` already emitted has always silently dropped in `SqliteStore::insertEdges()` - its `from` key never resolved to a real node. 1.8 disclosed this once ("not fixed, not worsened") and moved on. Every edge this rewrite's new compiled-PHP path produces would have been attributed to that same missing node and dropped exactly the same way, so this was a genuine prerequisite, not scope creep: `DirectiveScanner` now emits that `file:` node too (mirroring `PhpExtractor`'s own construction line for line), fixing both the pre-existing gap and giving the new edges somewhere real to land.

**A real, would-have-shipped-broken problem found the same way: Blade's own compiled runtime calls would have flooded `unresolved`.** Traced `ReferenceVisitor::record()` precisely: it only stays silent on an unresolvable receiver when that receiver is literally `$this` (`$direct`); every other unresolvable member call - including `$__env->make(...)`, `$attributes->except(...)`, `$component->...` - gets recorded. A real application with 650+ Blade views, most using `@extends`/`@section`/`@include`/component tags, would have added thousands of rows that are pure Blade-runtime plumbing, not real gaps - directly distorting `mapin:doctor`'s `unresolved_ratio` check and burying the signal `mapin:unresolved` exists to surface. Fixed with `BladeExtractor::filterRuntimeNoise()`, called by `BuildRunner` only for the Blade branch of its resolve pass: drops any `UnresolvedRow` whose `expression` starts with `$__env->`, `$attributes->`, or `$component->` - a narrow, explicit allowlist of known Blade-runtime variable names, not a broad heuristic (the same "denylist versus allowlist" lesson this project's own community/hub filter bug already taught it once).

**Known, disclosed limitation, not solved here: line numbers.** `BladeCompiler` has no source-map mechanism back to the original template at all - checked the vendored compiler directly; the only marker it adds is a single whole-file `/**PATH...ENDPATH**/` comment (and only via `compile($path)`, not `compileString()`), and component-tag expansion does not even preserve line counts across the transform. Edges from the compiled-PHP path carry whatever line the compiled PHP string put them on, not the original Blade source line. Building a faithful custom line-mapper is real, non-trivial work Laravel gives no support for; rather than attempt a fragile one, or touch the shared `Edge`/`UnresolvedRow` value objects to special-case a degraded line, this stays a disclosed imprecision - consistent with the project's existing "don't guess, say so" discipline rather than fabricating false precision.

**Verification.** Four new tests in `BladeCompilerTest.php`, against a dedicated fixture (`tests/Fixtures/blade-compiler/`, outside the shared golden fixture on purpose - the same precedent 1.17's `vendor-degradation/` set: a change that would add noise to the golden snapshot's diff gets its own small fixture instead): a template with a literal `route()`/`view()` call and an `@php` block calling a real fixture method produces real `links_route`/`renders`/`instantiates`/`calls` edges from the Blade file's own `file:` key; the shared golden fixture's own real structural directives (`@extends`/`@section`/`@include`/a component tag) never produce a single `$__env->`/`$attributes->` row in `unresolved`; `--no-boot` still falls back to `DirectiveScanner` (the `file:` node lands, no compiled-PHP edges do); a deliberately invalid `@php` block degrades to a build warning naming the file, never aborts the build (mirrors `PhpExtractor`'s own parse-failure precedent). The golden snapshot needed regenerating and was reviewed by hand, not blindly accepted: exactly the three shared-fixture Blade files' own new `file:` nodes, and their `Declares` edges now actually landing - no new `calls`/`renders`/`links_route` edges, because none of that shared fixture's three Blade files contain a literal `route()`/`view()`/method call. 116 tests total, PHPStan level 6 and Pint clean.

**Real-application verification, twice - the second run because the first one changed the plan.** Both against the real application's own copied-in package, never `composer.json`/`composer.lock` (this application depends on the published package, not a local path repository), `vendor/nexvia-solutions/mapin` deleted and reinstalled via `composer install` afterward both times.

First run, `--full` (4,671 files, needed - a brand-new capability against every existing Blade file, not an incremental touch): 452s, up from the roughly 5-minute baseline earlier full rebuilds in this project's own history report - compiling and resolving 650+ real Blade views for the first time is real, expected added cost, not a regression to chase down. 14,806 unresolved rows total. Broken down by receiver prefix before deciding anything: `$__env->` and `$attributes->` were correctly absent already (the planned filter), but `$errors->` (20.3% of every new Blade-sourced unresolved row) and `$__bag->` (28.7%) were not - together essentially half of the new noise, found only by actually looking at what a real application's real templates produce, not assumed from the design phase. Both are framework/Blade runtime, the identical reasoning that justified the first three: `$errors` is auto-shared into every view by `Illuminate\View\Middleware\ShareErrorsFromSession`, never application code; `$__bag` is `@error`'s own compiled local (confirmed by finding a real `@error(...)` usage and tracing what it compiles to). Added both to the same explicit allowlist, re-verified.

Second run, `--full` again (same 4,671 files, same reason - the filter change itself needed re-verifying against the same real data, not a subset): 445s. Unresolved dropped to 12,553 (the expected ~2,253-row reduction, matching the `$errors`/`$__bag` count from the first run almost exactly), confirmed zero remaining `$__env->`/`$attributes->`/`$component->`/`$errors->`/`$__bag->` rows by direct query. `unresolved_ratio` end to end: 12,553 / (93,947 + 12,553) ≈ 0.118 - *lower* than the 0.124 this same application reported before this change entirely (an unrelated incremental snapshot, not a perfectly matched baseline, but directionally clear): thousands of newly-resolved real edges from Blade content outweigh the residual genuine unresolved rows, this is a net improvement to the ratio `mapin:doctor` gates on, not a regression traded for new capability.

New edge types now exist from `.blade.php` files that never had any before beyond `includes`/`declares`: `calls` (16,135), `links_route` (2,285), `instantiates` (45), `resolves` (21), `uses_component` (30, now correctly attributing which backing class tag usage like `<x-simple-alert>` resolves to, a capability the structural regex path never had - Blade's own component-tag compilation for a class-based component genuinely does produce a real, literal PHP construct, unlike `@include`/`@extends`). One concrete example, verified by hand: `resources/views/frontend/authentication/reset.blade.php` line 35 (real source) writes `{{ route('frontend.home') }}` - invisible to `DirectiveScanner` regardless of boot state, never producing any edge before this change - now produces a real `links_route` edge to `route:GET /`.

The five build warnings this produced are worth reporting honestly rather than only citing the count: one (`app/Jobs/ProcessPadronImportJob.php`, a `.php` file, pre-existing, unrelated to this change) is `PhpExtractor`'s own parse-failure path, already covered by phase 2's own precedent. The other four are new, and three of those are not a limitation of this extractor at all - dumping the actual compiled PHP at the reported line (necessary, since the line number is compiled-space per the limitation above, not the original template's line) found three genuinely invalid PHP snippets already sitting in three real `.blade.php` files in the real application, each one a real bug that would break at Blade-compile time in production too, not something this change introduced: a `number_format(..., \',\', \'.\')` inside a `{{ }}` expression with JS-style backslash-escaped quotes that are not valid PHP syntax outside a string literal, and a `->map(fn ($count, $status) => [...])` with a missing closing `]` before the call's own closing `)`. `mapin:build` correctly reported each as a warning and kept building everything else - exactly the graceful-degradation path this change was designed to need, now confirmed necessary by real, not hypothetical, broken input. These three template bugs are outside this package's own scope to fix and were left exactly as found; not silently patched.

---

### 1.22 `mapin:warnings`, tracking what a build already finds and used to lose (built 2026-09-08, at Brandon's explicit request)

Verifying 1.21 against the real application surfaced five build warnings - three of them real, previously-unknown bugs in the real application's own Blade templates (1.21's own closing paragraph). Finding them required opening the raw `--json` output and reading the `warnings` array by hand; nothing about the existing design made that easy to do routinely, or as a team, rather than by accident during one specific verification pass. Asked directly afterward: "podria mapin tambien analizar sintaxis de codigo y reportarle este tipo de errores al usuario?" Answered narrowly and honestly rather than oversold: Mapin already detects this (a file failing to parse/compile, nothing broader - not type errors, not undefined variables, not style; PHPStan and Pint already exist and are better at that), the real gap is that the detection was ephemeral. The fix is `mapin:misses`'s own pattern (section 1.15), adapted for how a build warning actually recurs.

**Why this needed a different recurrence model than `query_misses`, not a copy of it.** `query_misses` appends a fresh row per occurrence - reasonable there, because nothing usually asks the identical missing symbol over and over in a tight loop. A build warning is the opposite: the *same* broken file produces the *identical* warning on every single subsequent build until someone actually fixes it, and with `mapin:install-hooks` (section 1.16) already wired into a real repository, that can be many times a day. Appending would flood the table with duplicates of the same three real problems, not surface new ones. `build_warnings.warning` is `UNIQUE`, and `SqliteStore::recordBuildWarnings()` upserts (`ON CONFLICT(warning) DO UPDATE SET last_seen_at = excluded.last_seen_at`) rather than inserts - one row per distinct problem, `last_seen_at` moving forward on every build it still occurs on, `first_seen_at` untouched from whenever it was first found. Schema bump to version 4 for the new table.

**Where it hooks in.** `BuildRunner::run()` already accumulates every warning any extractor produced into `$report->warnings` (unchanged); one new line right after the existing `$store->recordBuild(...)` call passes that same array to `$store->recordBuildWarnings(...)`. No extractor changed at all - this reads a signal that already existed, it does not create a new one.

**`mapin:warnings`**, mirroring `MissesCommand` closely (same `{--output=docs/mapin-warnings.jsonl}` default, same host-project export boundary - never inside this package's own `vendor/`, same "purely local, no network call ever" discipline, SPEC.md section 13 point 7): exports every row `exported_at IS NULL` to a JSONL file inside the *host* project, then marks those rows exported so a second run reports nothing new - unless the same warning genuinely recurred, whose `last_seen_at` moved forward without needing re-export (already exported once is still true; the row does not need seeing again just because the underlying file is still broken).

**Verification.** Nine new tests: five unit (`SqliteStoreTest.php`) proving the upsert behavior directly - the same warning recorded three times across three separate calls stays one row; `first_seen_at` survives a later call while `last_seen_at` moves (backdated directly via SQL rather than sleeping past `gmdate('c')`'s one-second resolution, still a real assertion, not a weaker one); two genuinely different warnings land as two separate rows; export-marking is scoped to the exact ids passed in. Reverted the `ON CONFLICT` clause alone (kept every other change) and confirmed the two upsert tests fail with a real `UNIQUE constraint failed` `PDOException`, not passing for the wrong reason - restored and re-confirmed green. Four feature (`BuildWarningsTest.php`, reusing the `blade-compiler` fixture's own `broken.blade.php` - already exercised in `BladeCompilerTest.php` for the graceful-degradation path itself, so no second broken fixture was invented just for this): a real build warning is recorded once, and a second full build hitting the exact same still-broken file does not duplicate it; `mapin:warnings` exports to the host project and reports nothing new on a second run. 122 tests total, PHPStan level 6 and Pint clean.

**Real-application verification**, against vendor-brandon's own real `--full` rebuild (438s, 4,671 files - both this feature and 1.21 verified together in one real run, since they ship in the same release; `vendor/nexvia-solutions/mapin` deleted and reinstalled via `composer install` afterward, same as every other real-application check this session). The five real warnings 1.21 already found (one pre-existing `.php` parse failure, three genuine bugs in real Blade templates, one restated from a prior run) landed as exactly five rows in `build_warnings` - confirmed directly against the real database, not inferred from the build's own JSON output alone. `mapin:warnings --json` exported all five to `docs/mapin-warnings.jsonl` inside the real project (`{"exported": 5, ...}`), each line carrying the real warning text and both timestamps; the exported file was deleted afterward along with everything else this verification touched, since it was never a real request, only a check that the mechanism produces a real, readable file in the real place. The two-builds-don't-duplicate behavior itself was not re-verified a second time at this same real scale on top of that - the upsert mechanism already has two independent real proofs (the reverted-`ON CONFLICT` unit test, and `BuildWarningsTest.php`'s two real `--full` builds against a genuinely broken fixture) that do not depend on database size to hold, so a second ~7-minute real rebuild solely to re-confirm the identical SQL behavior at larger scale was judged not worth its own cost.

**Not yet done, disclosed rather than assumed away:** this only ever tracks "the file failed to parse/compile at all" - a blunt, binary signal, not general syntax or style linting, and not something meant to compete with PHPStan/Pint, which already do that far better. No size cap or rotation on `build_warnings` or its exported JSONL, the same open question section 1.15 already left for `query_misses` - still not a real problem at the scale either has been seen at.

---

### 1.23 The MCP `_meta` fix from 1.19 was wrong about what "legacy" meant (found 2026-09-09, reported from a real client session)

Brandon updated `vendor-brandon`'s `composer.json` to the real `^0.2.2` tag and asked another live session to try the MCP connection. The handshake itself now worked (1.19's own fix) - a real improvement, confirmed independently - but every actual tool call, `stats` and `find` both, failed with `Invalid params: The request [_meta] is missing the required [io.modelcontextprotocol/protocolVersion] member`. Progress over 1.19 (the server used to reject the connection outright; now it connects and lists tools), but the same underlying symptom had just moved from the handshake to the first real call - not a different bug, the same one, still open.

**Root cause: 1.19's own fix used the wrong signal.** `MapinServer::validateProtocolMeta()` treated `_meta`'s outright absence as "this is a legacy client, skip strict validation" - reasoning that held for the fixture-driven tests that verified it (a hand-built classic `initialize` and a `tools/list` with no `_meta` at all), but not for a real client: `_meta` is a general-purpose bag that classic, pre-2026-07-28 clients already use for other things - a progress token on a `tools/call`, confirmed as the actual shape once reproduced (`_meta: {progressToken: ...}`, no `protocolVersion` key anywhere in it). A legacy request carrying *some* `_meta` for an unrelated reason is not the same as a modern request declaring a protocol version, but the old check could not tell them apart - both have non-null `_meta`, so both fell through to the base class's strict check, which then failed on the one key that was never going to be there either way.

**Fix.** Check the specific key the base class's own validation actually requires (`MetaKey::PROTOCOL_VERSION`, `"io.modelcontextprotocol/protocolVersion"`) rather than `_meta`'s presence as a whole: `$meta === null || ! array_key_exists(MetaKey::PROTOCOL_VERSION->value, $meta)` now both count as "not a modern request," legacy `_meta` content included. A request that does declare that key still goes through `parent::validateProtocolMeta()` unchanged - the strict, modern path 1.19's own test already covered is untouched.

**Verification.** One new test in `McpLegacyHandshakeTest.php`: a `tools/call` for `stats` carrying `_meta: {progressToken: 'abc123'}` and no `protocolVersion` now succeeds (previously the exact reported failure, reproduced first before writing the fix - reverting just this fix via a tagged git stash reproduces the identical error message the real report quoted, confirming this is the same bug, not a new one). 123 tests total, PHPStan level 6 and Pint clean.

**Real-application verification.** The fixed `MapinServer.php` copied into the real application's installed package; a full three-message session over stdio replicating the exact real failure - `initialize`, `notifications/initialized`, then `tools/call` for both `stats` and `find` (`{"name": "ServiceOrder"}`, the same tool and argument the real report used), each with `_meta: {progressToken: ...}` and no `protocolVersion` - both now return real results (`stats`'s real counts, `find`'s real `class:App\Models\ServiceOrder` node) instead of the error. `vendor/nexvia-solutions/mapin` deleted and reinstalled via `composer install` afterward.

**Not yet done:** this is now the second time a fix in this area was verified only against hand-built fixture requests and turned out to miss a real client's actual shape (1.19's own `_meta`-absence assumption before this). No further `_meta` usages are known to exist in current MCP clients beyond `protocolVersion`/`clientCapabilities` (the modern keys) and `progressToken` (confirmed here) - if another one surfaces from real use, the same fix shape (check the specific key that matters, not `_meta`'s presence) applies again.

---

### 1.24 `impact()` on a whole class silently returned almost nothing (found 2026-09-10, reported from real use)

A session reviewing documentation accuracy ran `mapin_impact` on a real controller class (`CartController`) to check what still depends on it, got `found: true` with `routes`/`methods`/`classes` all empty (only `docs` had real content), and guessed the cause was a Mapin limitation with array-callable route bindings - reasonable, but wrong, and disclosed as a guess rather than asserted as fact, which is exactly what led to it being checked here instead of quietly believed. Brandon asked directly: why, and can it be improved.

**Root cause, confirmed by querying the real database directly before touching any code.** The class in question has real data: 205 edges reference it, including 40+ real `routes_to` edges. But every one of them targets the class's own **method** nodes (`method:App\...\CartController::index`, etc.), never the class node itself - the only edge type that ever points at a class node directly is `documents` (a Markdown section mentioning the class by name). `Query::impact()`'s backward BFS seeds its frontier with exactly one id - the node passed in - so a class-key query only ever asks "what points at this exact class id," which for `routes_to`/`calls`/every edge type except `documents` is structurally always empty, for *any* class, not something specific to `CartController` or route-binding style. `found: true` with real doc mentions but a silently empty `routes`/`methods`/`classes` is a misleading answer, not an honest one - it reads as "nothing depends on this," not "this query shape doesn't reach what you're asking about," for a class real routes clearly do depend on.

**Fix.** When the resolved node is a `class` (`NodeType::ClassLike`), seed the BFS frontier with the class's own id *and* every method id it `declares` (one extra query, `edges.type = 'declares' AND target.type = 'method'` from the class), all at the same starting depth - a route or call reaching one of those methods now reports `depth: 1`, matching how a direct hit on the original seed already worked. Nothing else about the walk changed; `documents` edges targeting the class directly still contribute exactly as before, just no longer the only thing that does. SPEC.md's own requirements language (line 32) only ever promised `impact` for a *method* key - this was never a broken contract so much as a gap in what the contract covered, one intuitive enough that a real user hit it immediately.

**Verification.** One new test (`Phase2Test.php`): `mapin:impact` on a fixture controller's class key now reaches the real route registered against one of its methods, which it did not before. Reverted the fix via a tagged git stash and confirmed the test fails with the exact real symptom (`routes` empty), then restored it. 124 tests total, PHPStan level 6 and Pint clean.

**Real-application verification.** The fixed `Query.php` copied into the real application's installed package; `mapin:impact` on `class:App\Http\Controllers\Backend\Pos\CartController` (the exact class the real report used) now returns 60 routes and 18 methods, up from zero of either - `docs` (37) and the empty `views`/`jobs`/`commands`/`classes` buckets were unaffected, confirming the fix only ever adds what a class's own methods reach, nothing else. `vendor/nexvia-solutions/mapin` deleted and reinstalled via `composer install` afterward.

**A separately-flagged item turned out to already be resolved, not a second bug.** The same request asked to check `docs/mapin-misses.jsonl` (SPEC.md 1.15) for anything unresolved. One real entry existed: a `find` for `"App\Models\ServiceOrder"` (the full FQCN, not the short name) recorded `found: false` at `2026-09-08T21:59:34Z`. Reproduced against the current graph before assuming anything - it now resolves correctly (`found: true`), because that timestamp predates 1.18's own `findNodes()` ranking fix reaching this application's installed package; 1.18's fix covers full-FQCN searches equally well as short-name ones (same `ORDER BY`, `nodes.name = :name2` matches a full FQCN exactly), confirmed rather than assumed. No action needed - kept as a real, if now-historical, data point that 1.18's bug was slightly broader than its own write-up measured.

---

## 2. Architecture

Pipeline, run by `mapin:build`:

```
discover  ->  extract (per file, parallel-safe)  ->  index symbols  ->  resolve references  ->  store  ->  report
```

1. **Discover**: list files under configured paths, respecting `.gitignore` and `exclude` globs. Compute content hash. Compare with stored hashes to select changed, added and deleted files (incremental) or everything (`--full`).
2. **Extract**: each file goes through every `Extractor` that supports it. An extractor returns a `Fragment`: declared nodes, references (not yet linked) and unresolved expressions. Extractors never see other files.
3. **Index**: build the `SymbolIndex` (FQCN, methods, functions, view names, route names, table names, doc sections) from stored fragments plus the new ones.
4. **Resolve**: turn references into edges using the index, the type resolver and, when booted, the container bindings and the router. Every edge gets a `resolution` kind and a `confidence`.
5. **Store**: write nodes and edges to SQLite in one transaction per build. Deleted files remove their nodes and edges.
6. **Report**: write a `builds` row with counts, warnings, duration, commit. A graph that shrinks is stored anyway and the shrink is reported as a warning. Mapin never refuses to overwrite.

Extractors that need the application booted (routes, container bindings, Blade compiler) are skipped with a warning when `--no-boot` is used.

---

## 3. Graph model

### 3.1 Node types and keys

Every node has a unique `key` built from its type and a stable identifier. Keys are the public handle for every query.

| Type | Key format | Notes |
|---|---|---|
| `file` | `file:app/Services/Billing.php` | path relative to project root; `lang` in meta: php, blade, md, js |
| `class` | `class:App\Services\Billing` | kind in meta: class, interface, trait, enum, anonymous; `category` derived (see 3.4) |
| `method` | `method:App\Services\Billing::charge` | visibility, static, return type, line in meta |
| `function` | `function:money` | global functions |
| `route` | `route:GET /admin/orders/{id}` | name, action, middleware, domain in meta; searchable by name |
| `view` | `view:admin.orders.show` | Blade views; file linked via `declares` |
| `component` | `component:alert` | Blade class or anonymous component |
| `middleware` | `middleware:auth` | alias; resolved class in meta |
| `table` | `table:orders` | from models, migrations and `DB::table` |
| `doc` | `doc:docs/billing.md` | Markdown file |
| `section` | `section:docs/billing.md#refunds` | heading anchor |
| `concept` | `concept:refund` | only from the optional LLM layer |
| `external` | `external:Illuminate\Support\Facades\DB` | referenced class not found in the project |

Anonymous classes (Laravel migrations) get a synthetic key `class:anonymous@database/migrations/2024_01_01_create_orders.php` so their methods hang from a class node, not from the file.

### 3.2 Edge types

| Type | From -> To | Meta |
|---|---|---|
| `declares` | file -> class, function, view, doc; class -> method | |
| `extends` | class -> class | |
| `implements` | class -> class | |
| `uses_trait` | class -> class | |
| `calls` | method -> method or external | `member`, `resolution`, `confidence` |
| `instantiates` | method -> class | `resolution` |
| `injects` | class -> class (constructor); method -> class (method injection) | `param` |
| `binds` | class (abstract) -> class (concrete) | from container; `singleton` |
| `resolves` | method -> class | `app(X::class)`, `resolve()`, `App::make()` |
| `routes_to` | route -> method or class (invokable) | |
| `uses_middleware` | route -> middleware | |
| `renders` | method -> view | `view()`, `View::make()`, `response()->view()` |
| `includes` | view -> view | `directive`: include, includeIf, each, extends |
| `uses_component` | view -> component | |
| `links_route` | view or method -> route | from `route('name')` and `to_route()` |
| `relates` | class -> class (models) | `relation`: hasMany, belongsTo, ...; `method` |
| `maps_table` | class -> table | from `$table` or naming convention; `source` |
| `touches_table` | class or method -> table | migrations (`op`: create, alter, drop) and `DB::table` |
| `dispatches` | method -> class (job or event) | `via`: dispatch, event, Bus, Event facade |
| `listens` | class -> class (event) | from `$listen`, `Event::listen`, `#[ListensTo]`-style discovery |
| `observes` | class -> class (model) | `Model::observe()` and `#[ObservedBy]` |
| `schedules` | method -> class (command) or method | from `Kernel::schedule` and `routes/console.php` |
| `documents` | section -> any node | from Markdown mentions; `match`: class, path, route, table |
| `links_doc` | doc -> doc | Markdown links |
| `mentions` | section -> concept | LLM layer only |
| `relates_concept` | concept -> concept | LLM layer only; `source: llm` |
| `requests` | file (js) -> route | phase 7; `verb`, `url` |

### 3.3 Resolution kinds and confidence

Every `calls`, `instantiates`, `resolves` and `renders` edge records how the target was determined.

| Resolution | Meaning | Confidence |
|---|---|---|
| `static` | `Foo::bar()` with `Foo` resolvable | 1.0 |
| `new` | `new Foo()` or `(new Foo)->bar()` | 1.0 |
| `self`, `parent`, `this` | `self::`, `parent::`, `$this->bar()` | 1.0 |
| `typed_property` | `$this->svc->bar()` with declared or promoted type | 1.0 |
| `param` | `$svc->bar()` where `$svc` is a typed parameter | 1.0 |
| `container` | `app(Foo::class)->bar()`, binding resolved from the booted container | 1.0 |
| `constructor_assignment` | untyped property assigned from a typed constructor parameter | 0.8 |
| `local_assign` | local variable assigned from `new`, static factory or typed source in the same method | 0.8 |
| `return_type` | one hop through a method with a declared return type | 0.8 |
| `facade` | a facade's own `@method static` docblock names the return type | 0.8 |
| `cast` | an Eloquent `$casts`/`$dates` entry, or the default `created_at`/`updated_at`/`deleted_at` cast | 0.8 |
| `catch` | `catch (SomeException $e)` types `$e` for the block | 1.0 |
| `eloquent` | `Model::query()`, `Model::where(...)->first()` and similar chains, target is the model | 0.7 |
| `docblock` | `@var` or `@param` annotation | 0.7 |
| `interface` | call on an interface with a single implementation in the project, no binding found | 0.6 |
| `unique_member` | method name defined by exactly one class in the project (heuristic, off by default) | 0.4 |
| `unresolved` | none of the above; stored in `unresolved`, not as an edge | 0.0 |

A value carried through a local variable assignment (`local_assign`) or forwarded through a relation or facade keeps the resolution kind of its underlying source; confidence is the minimum of the source's confidence and the kind above, never higher than either alone. `catch` is 1.0, not 0.8, because PHP requires every catch type to implement `Throwable`, which is a language guarantee rather than an inference.

Query results report the minimum confidence along the path. Callers can filter with `min_confidence`.

### 3.4 Class categories

`category` is derived, never hardcoded to a directory: `controller` (extends a class named `Controller` or handles a route), `model` (extends `Illuminate\Database\Eloquent\Model`), `job` (implements `ShouldQueue` or uses `Dispatchable`), `event`, `listener` (appears as a listener), `middleware`, `command` (extends `Command`), `provider`, `migration`, `request` (extends `FormRequest`), `resource`, `policy`, `service` (fallback for classes under a namespace segment `Services`), `other`. The mapping is configurable in `config/mapin.php`.

---

## 4. Extractors

All extractors implement:

```php
interface Extractor
{
    public function supports(SourceFile $file): bool;
    public function requiresBoot(): bool;
    public function extract(SourceFile $file, ExtractionContext $ctx): Fragment;
}
```

| Extractor | Input | Produces | Boot |
|---|---|---|---|
| `PhpExtractor` | `*.php` under configured paths | classes, methods, functions, references (calls, new, static, injections, dispatch, relations, `view()`, `route()`, `DB::table`, `Schema::*`, `$listen`, `observe`) | no |
| `BladeExtractor` | `*.blade.php` | view node; compiles the template with the app's `BladeCompiler` and runs the PHP reference visitor on the output, so `@include`, `@extends`, components, `route()` and `view()` come from real PHP, not regex | yes (falls back to a regex directive scanner with `--no-boot`) |
| `RouteExtractor` | booted router | route nodes, `routes_to`, `uses_middleware` | yes |
| `BindingExtractor` | booted container | `binds` edges for abstract to concrete; feeds the type resolver | yes |
| `ScheduleExtractor` | `Console/Kernel.php`, `routes/console.php` | `schedules` | no |
| `MarkdownExtractor` | `*.md` | doc and section nodes, `links_doc`, `documents` (deterministic mentions) | no |
| `LlmConceptExtractor` | sections | `concept`, `mentions`, `relates_concept` | no, and only with `--with-llm` |
| `JsExtractor` | `*.js`, `*.jsx`, `*.ts`, `*.tsx` | `requests` edges by matching literal URLs to route URIs | no (phase 7) |

Extractors are registered through the service provider and can be added by the host app or by third-party packages (Livewire, Inertia, Filament adapters are explicitly out of the core and welcome as plugins).

### 4.1 Type resolution rules (PhpExtractor + Resolver)

Per class, the resolver builds a scope:

1. `$this`, `self`, `static`, `parent` from the class declaration.
2. Property types: declared types, promoted constructor parameters, untyped properties assigned in the constructor from typed parameters, `@var` docblocks.
3. Method scope: typed parameters, local variables assigned from `new`, static factories with declared return types, `app()`/`resolve()`, `$this->typedProperty`, and method calls with declared return types.
4. Interfaces: if the container has a binding, use it; otherwise, if the project has exactly one implementation, use it with `interface` resolution; otherwise unresolved with the candidates listed.
5. Eloquent chains: a static call on a model class returns a builder for that model; `first()`, `find()`, `create()`, `firstOrFail()`, `sole()` return the model; `get()` returns a collection (chain stops). Member calls on a builder are recorded as `calls` to the model with `eloquent` resolution when the model defines the method (scopes are matched as `scopeX`), otherwise as external Eloquent calls.
6. Facades are recorded as `external` targets and excluded from `impact` by default.
7. Docblock types are unwrapped before use, not read literally, because Illuminate and PHPStan-annotated userland code both rely on syntax a plain `@return Type` reader cannot parse:
   - **Conditional return types** (`@return ($key is null ? \Illuminate\Http\Request : mixed)`), used throughout the framework for helpers and methods whose result depends on whether an argument was passed (`request()`, `session()`, `Request::route()`, and dozens more). The resolver takes the `is null` true branch, because that is the no-argument case, which is overwhelmingly the case used for further chaining.
   - **Generic type parameters** (`@return \Illuminate\Database\Eloquent\Builder<static>`, `Collection<int, User>`), used across Eloquent's own source for `Model::on()`, `newQuery()`, and most builder-returning methods. The resolver strips the `<...>` suffix and resolves the base class; the type parameter itself is not modelled.
   - Both are Laravel and PHPStan-specific conventions, not general PHPDoc, and both must be handled for Eloquent chain resolution to work at all, since the framework's own base classes use them pervasively.
8. `catch (SomeException $e)` types the caught variable for the rest of the catch block at confidence 1.0. Any variable typed to a class, indexed or not, gets PHP's `Throwable` interface methods (`getMessage`, `getCode`, `getFile`, `getLine`, `getTraceAsString`, `__toString` as scalar-returning; `getPrevious` returning `Throwable` again, so a further `->getMessage()` still chains) resolved without needing the concrete exception class in the index, because every catchable type in PHP guarantees them by language contract, not by convention.
9. A small, explicit table of manager-to-contract forwarding is consulted when a direct method lookup fails on a manager or factory class: today this covers `Illuminate\Contracts\Auth\Factory` forwarding to `Illuminate\Contracts\Auth\Guard`, because Laravel's `AuthManager` implements `Factory` but answers `user()`, `id()`, `check()` and similar through `__call` delegation to the resolved guard. This is a named, documented quirk of a specific class, not a general fallback, and the table stays short by design; broadening it further belongs to phase 2 review, not an unlimited allowlist.
10. A bare class name with no matching `use` import and no project class of that name is checked against Laravel's own default facade aliases (`DB`, `Auth`, `Cache`, `Log`, `Str`, `Schema`, `Storage`, and the rest of the list Laravel registers via its `AliasLoader` from `config/app.php`) before being left unresolved. Code written as `\DB::table(...)` or, in an unnamespaced file, plain `DB::table(...)`, relies on that runtime `class_alias()` registration, which no import statement makes visible to static analysis; the table mirrors a fixed, documented list Laravel itself ships, not project-specific guessing.
11. Eloquent relation methods (`hasMany`, `belongsTo`, `hasOne`, `belongsToMany`, `morphMany`, `morphOne`, `morphToMany`, `hasManyThrough`, `hasOneThrough`) return a `Relation` subclass that forwards any method it does not itself declare to the query builder of the related model, via Laravel's `ForwardsCalls` trait. Reading the method body for `return $this->hasMany(Target::class)` (or the same call further chained, e.g. `->withDefault()`) gives the related model that a caller chaining `.where()`, `.get()`, `.count()` and similar actually reaches; a short, explicit list of relation-only mutators (`attach`, `sync`, `associate`, `save`, and similar) is excluded from that forwarding since they do not return a builder. The same relation information resolves the model accessed as a bare property (`$order->items`), which Eloquent's `__get` resolves to the related model or a collection of them depending on whether the relation is singular or plural, never to the `Relation` object itself.
12. A model's `$casts` and legacy `$dates` arrays are read for date-family cast types (`date`, `datetime`, `immutable_date`, `immutable_datetime`, and their `custom_datetime`/format-suffixed variants) and typed as `Carbon\Carbon` or `Carbon\CarbonImmutable`; these are attributes Eloquent's `__get` casts at runtime, never real PHP properties, so no other part of the source names their type. `created_at` and `updated_at` resolve to `Carbon\Carbon` on every model by the same default even with no explicit cast entry, and `deleted_at` does the same when the model uses `SoftDeletes`, because Eloquent applies these casts unconditionally, not by convention a project could deviate from silently.
13. Anything else becomes an `unresolved` row with the expression, receiver hint and member name, so it can be listed and improved later.

Heuristic resolution (`unique_member`) is disabled by default and enabled with `--heuristics`. It never silently upgrades to a confident edge.

### 4.2 Phase 0 measurements

Measured with a standalone script (`nikic/php-parser` plus the rules above, no package code) against `app/` of a real Laravel 11 application: 907 files, 911 project classes, indexed together with `Illuminate/*` and `nesbot/carbon` for return-type resolution. One file failed to parse (a `??` used in a position this php-parser version rejects) and was skipped and reported, matching the always-skip-and-report rule in section 13.

| Stage | Resolved at any confidence | Resolved at confidence >= 0.8 | Unresolved |
|---|---|---|---|
| Baseline (rules 1 to 6 only) | 49.7% | 26.9% | 13,684 calls |
| + rule 7 (conditional types, generics) and Carbon indexing | 76.7% | 46.9% | 6,346 calls |
| + rule 8 (catch typing) and rule 9 (auth forwarding) | 79.2% | 49.4% | 5,660 calls |
| + rules 10 to 12 (facade aliases, relation forwarding, casts) | 84.1% | 51.5% | 4,317 calls |

That is a 68% cut in unresolved calls (13,684 down to 4,317) across the two rounds of measurement, entirely from rules grounded in a documented Laravel or PHP behaviour confirmed against this application's real source before being coded, never from a guessed heuristic.

Restricted to member calls whose receiver resolves to a class declared in the project (the ones that actually feed `impact` and `callers`, 9,589 of the calls above): 100% resolve at confidence 0.7 or higher, 18.1% at confidence 0.8 or higher. The gap between 0.7 and 0.8 is not a resolver weakness: 60% of these calls are Eloquent chains, which section 3.3 caps at confidence 0.7 by design, because scope and macro resolution on a builder carries real uncertainty a typed property does not. This is why section 1.2 states the project-receiver criterion as two thresholds (0.7 and 0.8) instead of one, unlike the phase 0 gate's first draft, which asked for 80% of all calls at confidence 0.8 and was unreachable by the confidence model in section 3.3 itself, not by a shortfall in the extractor.

All three known failure cases from the previous tool resolved at confidence 1.0 throughout every stage above: a constructor-promoted-property injection (10 call sites), a service reached through both `app(X::class)->method()` and `new X()` in different controllers (13 call sites), and a facade-style class called statically from 20 different controllers (104 call sites).

Remaining unresolved calls (4,317) break down as: chains through a method whose return type is still unmodelled (1,984, the largest single bucket, mostly further Laravel/Illuminate return types and third-party packages such as a PDF generator, a spreadsheet writer and a server-side datatable library that a real installation would index alongside the app's own `vendor/`, but that this spike deliberately did not chase beyond Illuminate and Carbon), untyped parameters and untyped local variables (1,118 plus 764, a property of the code being analyzed, not of the resolver, and explicitly out of scope per section 1.3), static calls on an unindexed third-party class (106), and array element access (40, `$array['key']->method()`, out of scope per section 1.3). None of the remaining categories were found to be extractor defects; each was inspected against real source before being left unresolved.

---

## 5. Storage

SQLite file at `storage/mapin/graph.sqlite` (path configurable). Schema version stored in `meta`; a schema change bumps the version and triggers a full rebuild.

```sql
CREATE TABLE meta      (key TEXT PRIMARY KEY, value TEXT);
CREATE TABLE files     (id INTEGER PRIMARY KEY, path TEXT UNIQUE, lang TEXT, hash TEXT, size INTEGER, extracted_at TEXT);
CREATE TABLE nodes     (id INTEGER PRIMARY KEY, type TEXT, name TEXT, key TEXT UNIQUE, file_id INTEGER, line INTEGER, meta TEXT);
CREATE TABLE edges     (id INTEGER PRIMARY KEY, type TEXT, from_id INTEGER, to_id INTEGER, file_id INTEGER, line INTEGER,
                        resolution TEXT, confidence REAL, meta TEXT,
                        UNIQUE(type, from_id, to_id, file_id, line));
CREATE TABLE unresolved(id INTEGER PRIMARY KEY, file_id INTEGER, line INTEGER, kind TEXT, expression TEXT,
                        receiver_hint TEXT, member TEXT, candidates TEXT);
CREATE TABLE builds    (id INTEGER PRIMARY KEY, started_at TEXT, finished_at TEXT, commit_hash TEXT, mode TEXT,
                        files_seen INTEGER, files_changed INTEGER, files_affected INTEGER, nodes INTEGER, edges INTEGER,
                        unresolved INTEGER, warnings TEXT);
CREATE TABLE symbol_deps(file_id INTEGER, symbol TEXT, PRIMARY KEY (file_id, symbol));
CREATE TABLE query_misses(id INTEGER PRIMARY KEY, tool TEXT, args TEXT, suggestions TEXT, occurred_at TEXT, exported_at TEXT);
CREATE VIRTUAL TABLE nodes_fts USING fts5(name, key, content='nodes', content_rowid='id');
CREATE INDEX edges_from ON edges(from_id, type);
CREATE INDEX edges_to   ON edges(to_id, type);
CREATE INDEX nodes_type ON nodes(type, name);
CREATE INDEX symbol_deps_symbol ON symbol_deps(symbol);
```

A schema version bump drops and recreates every table (`Schema::reset()`) before `install()` runs, rather than attempting to migrate an old table's columns in place - a stale schema is a full rebuild by construction, exactly as this section always said, not merely as a fallback if a column happens to be missing.

**Incremental builds and cross-file invalidation (built and measured, see 1.4).** A file whose hash is unchanged keeps its nodes and edges - unless it depended on something a changed file altered. Every class, interface, trait or function `SymbolIndex` is asked about while resolving one file's references is recorded into `symbol_deps` for that file (`SymbolIndex::startTracking()`/`stopTracking()`, wired into `ReferenceVisitor`'s four call sites and into `record()`'s own interface-substitution and target-key lookup, so a dependency is captured regardless of which internal path touched the symbol). Before a changed file's old nodes are overwritten, its previously stored declared symbols (each class's FQCN, its interfaces, each function's name) are read and unioned with what the fresh extraction now declares; that union is the exact set of symbols this build changed, added or removed. `symbol_deps` is then queried for every other file whose last resolution depended on any of them - those files are re-parsed and re-resolved too, even though their own content and hash never changed, and this is reported as `files_affected` in the build report, separate from `files_changed`. A `--full` build always re-resolves everything and needs none of this. Measured against the application from 4.2: touching a file with 58 dependents (a static-utility class called from throughout the app) took 1.46 seconds incremental, correctly re-resolving all 58 affected files, still comfortably under the 5 second target from 1.2.

Deleted files cascade (their nodes, edges, unresolved rows and `symbol_deps` entries are removed via `ON DELETE CASCADE` when the `files` row is deleted).

`mapin:export` writes `graph.json` (nodes, edges, meta) or DOT or Mermaid for other tools. The JSON is an export, never the source of truth.

**`query_misses` (built 2026-09-08, at Brandon's request).** `Query::envelope()` writes one row here every time it answers `found: false` - never for a query whose result is merely an empty list, which stays `found: true` (see section 6's own `found`/`notFound` split). Purely local, same as every other table; `mapin:misses` (section 9, section 13 point 7) is the only thing that ever reads it, and only when a human runs the command on purpose. `exported_at` (NULL until exported) is what makes repeated runs of that command idempotent - a row only leaves once.

---

## 6. Query layer and MCP

One `Query` service backs the CLI and the MCP server. Every response follows the same contract:

```json
{
  "found": true,
  "query": { "tool": "impact", "key": "method:App\\Services\\Billing::charge", "depth": 3 },
  "result": { "...": "tool specific" },
  "confidence": 0.8,
  "graph": { "built_commit": "3b1c2cc4", "head_commit": "3b1c2cc4", "files_changed_since_build": 0, "built_at": "2026-09-06T14:02:11Z" }
}
```

Not found:

```json
{ "found": false, "query": { "...": "..." }, "suggestions": [ { "key": "class:App\\Http\\Controllers\\CartController", "score": 0.92 } ], "graph": { "...": "..." } }
```

Suggestions are labelled as such and are never returned as the result. A caller who wants fuzzy matching asks for it with `fuzzy: true` on `find`.

### 6.1 Tools (CLI subcommands and MCP tools share names and arguments)

| Tool | Arguments | Returns |
|---|---|---|
| `find` | `name`, `type?`, `fuzzy?` | exact node(s) by key, FQCN, short class name, route name or URI, view name, table name |
| `node` | `key` | node details, edge counts by type, file and line |
| `callers` | `key`, `depth=1`, `min_confidence=0` | who calls, injects, instantiates or resolves this node |
| `callees` | `key`, `depth=1` | what this node calls, renders, dispatches |
| `impact` | `key`, `depth=3`, `types?`, `min_confidence=0` | reverse reachability grouped by node type: methods, classes, routes, views, jobs, commands, docs; each item with path length and min confidence |
| `path` | `from`, `to`, `max_depth=6` | shortest edge path |
| `route` | `uri` or `name` | route, handler chain, middleware, views rendered, models touched |
| `view` | `name` | who renders it, what it includes, components, routes it links to |
| `model` | `name` | table, relations, migrations, observers, usages |
| `unresolved` | `file?`, `member?`, `limit` | unresolved references, for improving code or the resolver |
| `hubs` | `limit`, `types?` | highest degree nodes, facades excluded |
| `communities` | `resolution?` | community membership summary; requires `mapin:communities` to have run |
| `docs` | `key` | sections that document the node, and nodes documented by a section |
| `stats` | | counts, build history, staleness |

The MCP server is exposed through `laravel/mcp` as a local (stdio) server: `php artisan mapin:mcp`. Tools are defined against Mapin's own `ToolSpec` interface and adapted to `laravel/mcp` in `src/Mcp/Adapters/`. Swapping the MCP library must not touch `src/Query` or `src/Mcp/Tools`.

---

## 7. Markdown module

Separate extractor, same graph. Two layers:

**Deterministic (always on):**
- `doc` node per file, `section` node per heading with anchor.
- `links_doc` for relative Markdown links.
- `documents` edges from mentions in the section text: backticked or plain FQCNs and short class names that exist in the index, file paths, route names, table names, view names, URIs matching a route. Match type recorded in meta. Short-name matches with more than one candidate produce an unresolved row, not a guess.

**Semantic (opt-in, `mapin:docs --with-llm`):**
- Sends section text (never PHP source) to the configured LLM driver to extract concepts and relations between concepts.
- Driver behind `Mapin\Extract\Markdown\Semantic\LlmClient`; a `NullClient` is the default. Provider drivers live in their own namespace and are configured in `config/mapin.php`. Implementation of any provider driver must follow that provider's current API reference at the time of coding.
- Results are stored with `source: llm` and a model identifier, and are dropped and regenerated per section when the section hash changes.

---

## 8. Analysis module

Runs on the stored graph, never during extraction.

- **Communities** (`mapin:communities`): Louvain implemented in PHP over the undirected projection of `calls`, `injects`, `instantiates`, `routes_to`, `renders`, `includes`, `relates`, `dispatches`, `listens`. External nodes and facades are excluded, hub nodes above a degree percentile are excluded and re-attached to the community of their strongest neighbour. Deterministic node ordering so two runs on the same graph give the same result. Membership stored in `nodes.meta.community`; a summary (size, top nodes, dominant namespaces) is written to `meta`.
- **Hubs**: degree and betweenness-lite (bridge count between communities).
- **Staleness**: `built_commit` versus `git rev-parse HEAD` and `git diff --name-only`, reported in every response.

---

## 9. Commands

```
php artisan mapin:build        [--full] [--no-boot] [--paths=app,routes,...] [--heuristics] [--json]
php artisan mapin:query        {tool} [--arg=value ...] [--json]        # any tool from section 6.1
php artisan mapin:find         {name} [--type=] [--fuzzy]               # convenience aliases for the most used tools
php artisan mapin:impact       {key} [--depth=3] [--min-confidence=0]
php artisan mapin:callers      {key} [--depth=1]
php artisan mapin:route        {uri-or-name}
php artisan mapin:unresolved   [--file=] [--member=] [--limit=50]
php artisan mapin:stats
php artisan mapin:communities  [--resolution=1.0]
php artisan mapin:docs         [--with-llm]
php artisan mapin:export       [--format=json|dot|mermaid] [--out=]
php artisan mapin:mcp                                                   # stdio MCP server
php artisan mapin:doctor                                                # dependencies, boot, staleness, unresolved ratio
php artisan mapin:misses       [--output=docs/mapin-misses.jsonl]       # export found:false queries to a host-project JSONL file
php artisan mapin:install-hooks [--command="php artisan mapin:build"]   # install post-commit/post-merge hooks that rebuild in the background
php artisan mapin:clear
```

Every command supports `--json` for machine consumption. Exit code 1 on build errors, 2 on staleness warnings when `--strict` is passed (for CI).

`mapin:install-hooks` (section 1.16) installs `post-commit`/`post-merge` hooks that rebuild in the background - opt-in, not run automatically by anything else in this package.

---

## 10. Project structure

```
mapin/
├── composer.json                 # nexvia-solutions/mapin, PHP ^8.2, illuminate/* ^11.45|^12|^13, nikic/php-parser ^5.0, laravel/mcp
├── LICENSE                       # MIT
├── README.md  README.es.md  CHANGELOG.md  CONTRIBUTING.md  SPEC.md
├── config/mapin.php              # paths, exclude, storage path, boot, heuristics, categories, llm driver, communities
├── src/
│   ├── MapinServiceProvider.php
│   ├── Console/Commands/         # one class per command in section 9
│   ├── Graph/                    # Node, Edge, NodeType (enum), EdgeType (enum), Resolution (enum), Confidence, Key
│   ├── Store/                    # SqliteStore, Schema, SchemaMigrator, BuildReport
│   ├── Extract/
│   │   ├── Contracts/            # Extractor, Fragment, ExtractionContext, SourceFile
│   │   ├── Discovery/            # FileDiscovery (gitignore aware), Hasher
│   │   ├── SymbolIndex.php
│   │   ├── Php/                  # PhpExtractor, Visitors/DeclarationVisitor, Visitors/ReferenceVisitor, Scope, TypeResolver, LaravelHints
│   │   ├── Blade/                # BladeExtractor (compiler based), DirectiveScanner (no-boot fallback)
│   │   ├── Routes/               # RouteExtractor
│   │   ├── Container/            # BindingExtractor
│   │   ├── Schedule/             # ScheduleExtractor
│   │   ├── Markdown/             # MarkdownExtractor, Semantic/{LlmClient, NullClient, LlmConceptExtractor, LlmClientFactory, Drivers/OllamaClient}
│   │   └── Js/                   # JsExtractor (phase 7)
│   ├── Resolve/                  # Resolver (second pass), Candidates
│   ├── Query/                    # Query, Finder, Traversal, Impact, Response
│   ├── Analysis/                 # Louvain, Hubs, Staleness
│   ├── Mcp/                      # Contracts/ToolSpec, Tools/*, Adapters/LaravelMcpAdapter
│   └── Support/
├── tests/
│   ├── Unit/                     # resolver rules, keys, Louvain, mentions
│   ├── Feature/                  # build, incremental, query contract, MCP tools, commands
│   ├── Fixtures/app/             # invented Laravel app (Orchestra Testbench) covering every pattern in 3.2 and 3.3
│   └── Golden/                   # expected nodes and edges for the fixture app
├── docs/                         # ARCHITECTURE.md, GRAPH_MODEL.md, MCP.md, BENCHMARKS.md, EXTENDING.md
└── .github/workflows/ci.yml
```

---

## 11. Code style

- PHP 8.2+, `declare(strict_types=1)` in every file, PSR-12 enforced with Laravel Pint (`laravel` preset).
- Classes `final` by default; value objects `readonly`; enums for node types, edge types and resolution kinds. No static mutable state.
- PHPStan level 8 from day one (baseline file allowed only for third-party typing gaps).
- One class per responsibility; extractors, visitors and tools stay under about 300 lines. Long switch statements over node types go into small strategy classes.
- No hardcoded directories, framework paths or class names beyond the Laravel base classes listed in 3.4. Everything else is discovered through the container, the router or configuration.
- Identifiers, comments, commit messages and docs in English. Conventional Commits (`feat:`, `fix:`, `docs:`, `test:`, `refactor:`, `chore:`).
- No em dash or en dash characters in any generated text (code comments, docs, commit messages, CLI output).
- Errors are values where possible: extraction failures become warnings in the build report, never uncaught exceptions that abort the whole build. A syntax error in one file skips that file and records it.

---

## 12. Testing strategy

- **Framework:** Pest on Orchestra Testbench. CI matrix: PHP 8.2, 8.3, 8.4 by Laravel 11, 12, 13. Pint `--test`, PHPStan and Pest must pass on every push.
- **Fixture app:** an invented domain (for example a library or a bike shop) with at least one instance of every pattern in 3.2 and 3.3: constructor injection typed and untyped, method injection, interface bound in a provider, interface with a single implementation and no binding, `new`, static factory, Eloquent chains, scopes, relations of every kind, `$table` override and convention, migrations with anonymous classes, routes with names, groups, resources and invokable controllers, middleware aliases, Blade layouts, includes, `@each`, class and anonymous components, `route()` inside Blade and PHP, jobs, events, listeners via `$listen` and via `Event::listen`, observers, scheduled commands, Markdown docs with backticked and plain mentions, links and ambiguous short names. Nothing in the fixture refers to any real product, company or tenant.
- **Golden tests:** `tests/Golden/expected.json` lists every node key and every edge (type, from, to, resolution) the fixture must produce. The build output is diffed against it. Any change to the golden file is reviewed in the PR.
- **Resolver unit tests:** table driven: a PHP snippet, the expected target and resolution kind. One row per rule in 4.1, plus negative rows that must produce `unresolved`.
- **Contract tests:** every tool returns the envelope in section 6; not-found returns `found: false` with suggestions; `fuzzy: false` never returns a non-exact node; every response includes `graph` staleness.
- **Incremental tests:** touch one fixture file, assert only that file is re-extracted; delete a file, assert its nodes and edges are gone; make the graph shrink, assert the build succeeds with a warning.
- **MCP tests:** tool list and schemas match `ToolSpec`; a request through the adapter returns the same payload as the CLI.
- **Benchmark (manual, not CI):** `bin/benchmark` runs a full and an incremental build on any real app and prints aggregate numbers (files, nodes, edges, resolution ratio per kind, duration). Results are copied into `docs/BENCHMARKS.md` as numbers only.
- **Coverage targets:** `Extract/Php` and `Resolve` at 90% or higher, overall 80% or higher.

---

## 13. Boundaries

### Always

- Return `found: false` explicitly. Suggestions are labelled and never substituted for the result.
- Attach `resolution` and `confidence` to every resolved edge and report the minimum confidence on every traversal.
- Run fully local by default. No network calls unless `--with-llm` is passed.
- Build incrementally by content hash. Store the result even when the graph shrinks, and report the shrink.
- Report staleness (`built_commit`, files changed since build) in every response.
- Keep extractors behind the `Extractor` interface so host apps and plugins can add their own.
- Derive categories and paths from the container, router and config. Never assume `app/Models` or any other directory layout.
- Skip and report a file that fails to parse; never abort the build for one file.
- Pass Pint, PHPStan and Pest before every commit. Use Conventional Commits.
- Keep fixtures, docs and examples invented and generic.

### Ask first

- Adding any runtime dependency to `composer.json`.
- Changing the SQLite schema (requires a schema version bump and a documented full rebuild).
- Adding an extractor that requires booting the host application in a new way.
- Renaming or changing the arguments of a query tool after the first tagged release.
- Tagging a release, publishing to Packagist, or changing CI secrets.
- Enabling any heuristic resolution by default.

### Never

1. **Never send code to an LLM without an explicit flag.** Extraction is local. Only the optional semantic Markdown layer calls a provider, only with `--with-llm`, and only with Markdown section text, never PHP source. Reason: the package runs inside third-party proprietary codebases; a silent upload is a security incident and destroys trust.
2. **Never write outside the configured storage directory** (default `storage/mapin/`). No files in the project root, no edits to `.gitignore`. Reason: a tool that pollutes the host working tree produces stale duplicate graphs and untracked noise, which is exactly what went wrong with the previous tool. **One deliberate exception:** `mapin:misses`, and only when a human runs it - see point 7.
3. **Never execute the host application's business logic.** Booting the kernel to read the router and the container is allowed. Dispatching requests, running jobs, running commands, or executing a single Eloquent query against the host database is not. `--no-boot` disables booting entirely. Reason: the tool may run against production-like databases.
4. **Never include data from any real project or tenant in the repository.** Fixtures are invented. Benchmarks are published as aggregate numbers. Reason: public repository; internal names and URLs are a leak and make the tool look bespoke.
5. **Never answer with an approximate match without saying so.** Reason: the previous tool returned look-alike nodes with full confidence and callers acted on them.
6. **Never leave a stale graph silently.** Every build writes a report, a shrinking graph is a warning not a refusal, and every response carries the build commit. Reason: silent refusals to overwrite left graphs several commits behind without anyone noticing.
7. **Never phone home, under any circumstance, for any installation.** `Query::envelope()` records every `found: false` answer to `query_misses`, a table in this project's own `graph.sqlite` - purely local, exactly like every other table. `mapin:misses` (the one exception to point 2) only ever reads that local table and writes a JSONL file inside the *host* project, never inside this package's own `vendor/` directory, and only when a human runs the command on purpose - never during `mapin:build`, never during any query. No part of this package makes a network call outside `--with-llm` (point 1). Reason: this package is published to anyone on Packagist; a third party's own private code names, routes or gaps must never reach us, on purpose or by a design mistake.

---

## 14. Phases and acceptance gates

| Phase | Deliverable | Gate to pass before the next phase |
|---|---|---|
| 0. Spike (done, see 4.2) | Standalone script using php-parser over a real `app/` directory, emitting `injects`, `instantiates`, `calls` with resolution kinds and an unresolved list. Run inside the host app container. | Met: 100% of project-receiver calls resolved at confidence 0.7+, 18.1% at 0.8+; 84.1% of all member calls resolved at any confidence, 51.5% at 0.8+ (up from a 49.7%/26.9% baseline, a 68% cut in unresolved calls). All three known failure cases from the previous tool resolved at confidence 1.0. Rules 7 to 12 in section 4.1 are carried forward into the real `PhpExtractor`/`Resolver` as requirements, not left behind in the spike script. |
| 1. Core (built, see 1.4) | Package skeleton, service provider, config, `FileDiscovery`, `PhpExtractor`, `SymbolIndex` (with dependency tracking), `Resolver`, `SqliteStore` (with `symbol_deps`), `BuildRunner` (with affected-file detection), `mapin:build` full and incremental, build report, `mapin:stats`, `mapin:find`, `mapin:callers`, `bin/benchmark.php`. Fixture app and unit/feature tests covering every rule in 4.1, plus a dedicated cross-file invalidation test. | Met against the real application from 4.2: full build of 3,596 files (project plus indexed vendor) in 33 to 43s; incremental with no changes in 0.14 to 0.87s; incremental touching a file with 58 dependents in 1.46s, correctly re-resolving all 58. All 14 tests green (one written to fail without the fix, confirmed to actually fail with it disabled), Pint and PHPStan level 6 clean. Cross-file incremental invalidation, the gap this phase originally shipped with, is closed - see 1.4. CI workflow written, not yet run on real GitHub infrastructure. |
| 2. Laravel (built, see 1.5) | `RouteExtractor`, `BindingExtractor`, `BladeExtractor` (regex based, not compiler based - see 1.5), relations, tables, migrations, dispatch, listeners, observers, schedule. `mapin:route`, `mapin:view`, `mapin:model`, `mapin:impact`. | Met: `impact` lists routes/views/jobs/commands/methods/classes in one call via a one-hop heuristic; 1,589 of 1,589 routes present against a real, booted application's router, exactly - not approximately (27 tests green, Pint and PHPStan clean; see 1.5). Golden-test apparatus built in the 1.8 hardening pass, after this phase closed - see 1.8, not still open as this row originally said. |
| 3. MCP (built, see 1.7 and 1.9) | `ToolSpec`, tools for every query, `laravel/mcp` adapter, `mapin:mcp`, `mapin:doctor`, `docs/MCP.md` with Claude Code and Cursor setup. | Met: not-found contract verified from a real `tools/call` round-trip against both the fixture (1.7) and a real application's graph (1.9); `mapin` is a real, working entry in that application's own `.mcp.json`, checked by running the exact configured command line with a real request, not just declared (see 1.9). Installed against a Laravel 13 upgrade branch, not that application's main working branch (still Laravel 10) - see 1.9's closing note. |
| 4. Markdown | Deterministic `MarkdownExtractor`, `documents` and `links_doc`, `mapin:docs`, `docs` tool. | Every section mentioning a known class or route is linked; ambiguous short names land in `unresolved`. |
| 5. Analysis | Louvain in PHP, hubs, `mapin:communities`, `communities` and `hubs` tools, deterministic output. | Same graph, same result across runs; communities align with known modules of the benchmark app in a manual review. |
| 6. Semantic docs | `LlmClient` contract, `NullClient`, one provider driver, `--with-llm`, per-section caching by hash. | Concepts regenerate only for changed sections; zero network calls without the flag (asserted by test). |
| 7. JS | `JsExtractor` matching literal URLs in `fetch`/`axios` calls to route URIs; `requests` edges included in `impact`. | Routes called from JS appear in `impact` for their handlers. |
| 8. Release | README, README.es.md, CONTRIBUTING, CHANGELOG, `docs/EXTENDING.md`, GitHub repo public, Packagist, v0.1.0 tag. | Fresh install on a clean Laravel 11 and 13 app works from the README alone. |

---

## 15. Risks and mitigations

| Risk | Mitigation |
|---|---|
| `laravel/mcp` is in beta and its API may change | Tools are defined against `ToolSpec`; only the adapter depends on the library. `php-mcp/laravel` is the documented fallback. |
| False positives in type resolution (interfaces, chains) | Confidence on every edge; heuristics off by default; `unresolved` is a first-class output, not a silent drop. |
| Blade compilation requires the app's view compiler | `--no-boot` falls back to a directive scanner with lower confidence; documented. |
| Performance on large apps | Single pass per file, fragments cached by hash, resolve pass in memory, SQLite in one transaction. Benchmark script from phase 1. |
| Scope creep toward a full static analyser | Non-goals in 1.3 are explicit. Anything requiring data flow across methods is out. |
| Maintenance burden after publishing | Small core, plugin interface for framework add-ons, golden tests that make regressions visible, CI matrix. |

---

## 16. Open questions

- Default depth for `impact` (3 proposed) and whether facades should be included on request.
- Whether `mapin:build` should install the recommended git hook on demand (`--install-hook`) or stay documentation only.
- Storage of an optional JSON export on every build for tools that cannot read SQLite (proposed: no, export on demand).
- Community detection resolution default (1.0 proposed) and the hub exclusion percentile (top 1% proposed).
